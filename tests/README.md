# Tests / 测试

## 中文

船体隧道受光回归分别验证驻留道路采样、运行时传递和真实船体材质扩展：路线/profile 失配、不可见与处置后的照度必须清零，洞口连续，Low/Medium 不枚举 High 灯具；所有实体材质及损伤变体共享稳定 uniform，经 PBR 漫反射接收且不改自发光或高光。浏览器仍须观察灯下船体颜色与轮廓，不能把 shader 文本检查当成实际画面验收。

道路边缘相机回归必须连续执行生产跟随阻尼、FOV、侧倾与入镜保护顺序，覆盖左右边缘停留、往返横移及不同帧率。固定道路、船体与 HUD 后，观察角度和 FOV 必须收敛，不能在仍然 `fits=true` 时周期性跳变；独立验证最终纠偏不污染下一帧跟随状态，侧倾也不得逐帧累积。

小窗口与负载回归直接执行生产 HUD 投影、安全矩形和相机函数：稳定 `600` 帧统计重复 DOM/翻译写入，隐藏/展开、语言切换、快速输入与计时到期必须保持即时；相机使用真实 Three 船体包络与多尺寸、坡度、滚转、腾空组合投影。结构测试保留全部批次和材质，并校验移动实例包围球覆盖实际顶点、主视锥与阴影视锥独立工作；纹理发布版本覆盖等数量替换与处置后回调。离线调用耗时不能直接等同游戏 FPS。`browser/*hud-layout-test.html` 为独立布局测试入口；生产局域网服务器有意不发布 `tests/` 且禁止 iframe，实机验收使用生产页面本身调整窗口尺寸与只读调试 DOM，不能为运行夹具放宽服务器边界。

长航程路网回归由 `integration/Neon_Autopilot_V23_HighSpeed_DroneHeat.topology-lifecycle.test.js` 在 20km 明确检查点后继续跨越 100 个真实图块，并在每图块构造直行/右转/左转三候选扇出。它要求当前/峰值 registry、净空与可见道路扫描和 token 总字符数保持上界，最大 token 不超过 `55` 字符；同时逐图块验证当前路、Film 向后采样、向前采样、地图可见边和完整飞船落地承托。末尾用窗口外恢复源证明只保留紧邻目的图块而不递归保留更远未来。

空中落地联合承托矩阵遍历四个入口与三种移动的全部 `256` 个连续 PathPlan 接缝，并在同图块、跨图块接缝的中心及 `±0.5m / ±1.359m` 处要求完整船长命中 `connected-road-seam`；下一图块未激活和中心越过真实终点仍必须返回空，防止修复退化为幽灵路面。合流处另以横向 `3.8m` 的重叠支路样本锁定当前计划优先，避免亚微米投影噪声触发可选支路采用。

- `unit/`：单模块纯逻辑与资源合同。
- `integration/`：道路、地图、生成与跨模块组合。
- `contracts/`：入口、运行时、样式、更新提示和说明同步。
- `browser/`：真实页面、WebGL 与响应式尺寸夹具；Node 通过不能替代这里的视觉验收。

统一入口为项目根目录执行 `node tools/verify-node.mjs`。

跨批次景物净空验收由 `integration/Neon_Autopilot_V23_HighSpeed_DroneHeat.scenery-clearance.test.js` 使用固定随机种子和真实生产几何/API 统一锁定：High `16` 与 Mobile `10` 六境地形对全部道路胶囊零穿入，可见世界根零非语义悬浮/埋入，同行者真实表面间距，全部可见道路结构阻挡记录的有限绝对坐标与 Film `0.35m` 解析余量，水/雪毫米级贴路与雪丘平底，`18` 个生物家族的非安装缝零自交，以及 Film 圆柱纯函数输入矩阵与调用方输出复用。桥墩—柱冠—横梁等真实承重接缝允许各自的保守圆柱互相重叠，测试验证每个圆柱包住真实矩阵八角并可被镜头解析，不能错误要求结构组件彼此分离。透明特效只能通过生产名称精确白名单豁免，不得用类型或材质模糊放行。

生成回归以 `600s` 停车和低速步进证明障碍、船流、烛光三条时钟只随真实路程推进；三条通道在低于 `24m/s` 时统一按 `speed / 24` 缩放，速度为 `0` 时距离与时钟都完全冻结。障碍工厂测试同时锁定家族可用容量在取得/释放前后的变化。运行时合同要求静态/动态家族 `48 / 24` 根、单事件最多三根的整组预检、晚到岔路与对向副本的独立预检，以及三类容量抑制诊断，防止对象池耗尽再次中断长时间游玩。

Windows 启动回归由 `server/windows-local-server.test.mjs` 锁定完整启动链：纯 ASCII、无 BOM、CRLF 的 `Start-V23-Windows.cmd` 与中文别名，UTF-8 BOM、CRLF 的 Windows PowerShell 5.1 服务脚本，任何退出码共用的留窗路径，TEMP 诊断日志、浏览器回退，以及第 2 版清单对主 HTML、两个 CMD、PS1 和全部运行资源的逐字节覆盖。非 Windows 上的 Node 通过只证明静态字节与源码合同；Windows PowerShell 5.1、cmd.exe、默认浏览器和真实 loopback 请求仍需目标 Windows 冒烟。

双语回归由 `unit/Neon_Autopilot_V23_HighSpeed_DroneHeat.i18n.test.js` 锁定同构词典、默认简体中文、查询参数/安全存储优先级、切换持久化、动态节点重放、静态入口覆盖、英文零汉字与中文零英语 UI 术语，并要求独立硬件 token `A / D / W / SPACE / S / M / Q / E / T` 在两种语言及往返切换后保持原样；地图“分流/汇流”仍通过明确语义键翻译。启动页合同另要求六个弹层语言入口全部采用紧凑样式、HUD 内为零入口，并保持 `44 × 44px` 实际触控区与约 `34 × 28px` 可见胶囊。真实浏览器矩阵须分别以 `?lang=zh-CN` 和 `?lang=en` 打开页面，在 `320 × 568` 竖屏、`844 × 390` 矮横屏及桌面尺寸检查可见文本与无障碍属性的语言纯度、弹层内即时切换、开始后动态 HUD/地图更新，以及所有叶节点边界和水平溢出；只检查文档 `scrollWidth` 不足以证明通过。

全屏单元回归要求开始页右上工具键与 HUD 全屏键消费同一整页状态：初始化、语言更新与 `fullscreenchange` 不得自动请求，只有玩家主动点击才可进入或退出；两枚控件同步可访问名称、`aria-pressed`、状态文字与能力禁用，且全屏能力不得取得或推进启动 action 的 `ready / committing / commit`。浏览器布局夹具只证明开始页工具键的 `44px` 目标、卡内收纳和零重叠；可信全屏手势仍须在直接页面验收。

启动页静态回归由 `contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.launch-screen.test.js` 锁定唯一初始 loading surface、外层非 live、唯一阶段 polite 状态通道、不可聚焦的 `role="note" aria-live="off"` 提示、初始 `hidden + inert` 的开始页、原生禁用的开始按钮，以及模块/世界/道路/图形/首帧五个无伪百分比阶段；同时保留唯一模态根、舞台/卡片/简报/配置/操作顺序、装饰 `aria-hidden`、原生天气与画质控件、响应式断点、减少动态，以及 `launch / pause / gameover + ready / committing` 钩子。i18n 回归锁定 `tipLabel` 与 steering / view / ramps / candlelight / damage / weather 六个中英稳定 ID，不得出现百分比或 ETA。源码顺序合同必须证明提示只在真实阶段严格前进时换条，重复阶段不换，不使用 timer 或玩法 RNG，不写 readiness；普通入口仍按“启用 Start → 显示开始页 → 隐藏 loading”原子发布，并证明更新提示高于 loading、致命恢复高于二者。开始页右上全屏工具键保持在启动 action 之外，不改变暂停或结算动作区。暂停合同另锁定仅暂停可见的“重新开始本局”、`R` 快捷键、独立二级 `alertdialog`、默认取消焦点、键盘圈闭，以及只有确认按钮可进入唯一 `resetGame()`；取消与 `Esc` 不得改动暂停状态或本局进度。真实页面验收还需确认玩家第一次看见开始页时按钮已经可用、启动 action 与全屏工具键均至少 `44px`、确认后的新局状态、首个成功帧后撤层和焦点返回画布。浏览器矩阵继续验证提示卡在手机窄竖屏、矮横屏和 iPad 横竖屏不溢出，减少动态直接替换，强制颜色仍有文字分类与系统色边界；其余双击、配置锁定、模式文案与焦点圈闭合同保持不变。

飞行焦点回归由 `accessibility-smoke.test.js` 精确枚举桌面 HUD `12` 个、移动 HUD `13` 个与触控驾驶 `5` 个按钮，要求全部保留语义但使用 `data-flight-deck-control + tabindex="-1"`；启动、暂停、结算、手册、曲库、确认、更新和恢复按钮不得继承该标记。运行时合同锁定模态守卫先行、飞行态 `Tab / Enter / NumpadEnter` 拦截、四方向键与 `SPACE` 取消浏览器默认后继续驾驶、持续触控键无目标级键盘监听，以及曲库关闭和异常 `focusin` 回到 Canvas。布局夹具另检查 `30` 个唯一控件、`12 + 13 + 5` 所属数量、顺序焦点排除和飞行中曲库关闭后的 Canvas 焦点；合成事件不能证明浏览器原生 click 未发生，最终仍需用真实键盘在实际浏览器验收 Enter、Space、方向键和 Tab。

手册同步合同由 `contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.guide-sync.test.js` 单独拥有：第 `18` 版必须恰好包含 `43` 个唯一玩家功能 ID，`weather-manual-test` 不得进入账本，世界章五张装饰预览仍使用独立钩子。静态与运行时源码断言锁定默认 Essential/必会层显示 `16` 项、Complete/完整层显示同一 DOM 账本的全部 `43` 项、打开时冻结投影的阶段/横纵权威/速度/生命与烛光/制动/下一步、手机紧凑摘要、五步 60 秒快速上手、P 优先接管、M 保存满油门偏好的四状态矩阵、循环上一章/下一章、切章回到内容顶部，以及关闭/返回/`Esc` 撤下手册覆盖层并恢复实际开启者焦点。CSS 合同分别检查非矮横屏手机四章单行、矮横屏竖向侧栏、单列内容和隐藏冗余返回控件，竖屏小 iPad 全高外壳与 `180px` 侧栏、唯一滚动区和所有可见操作目标。布局夹具已经加入 v18 专用断言：Essential 显示 `16` 项并隐藏 `27` 项参考内容、完整层显示全部 `43` 项、只读上下文有 `6` 个字段、手机响应式章节方向和紧凑摘要、手机返回控件隐藏、上一章/下一章双向循环、四章末项可达、唯一滚动区、正文至少 `12px`，以及可见操作目标至少 `44 × 44px`；iPad 尺寸包含竖屏 `768×1024 / 820×1180 / 834×1194 / 1024×1366` 和横屏 `1024×768 / 1180×820 / 1194×834 / 1366×1024`。静态合同只证明断言存在，必须另行执行浏览器夹具；浏览器夹具仍不能替代真实 iPad Safari 的安全区、动态浏览器栏、旋转和触控设备验收。

启动更新提示与开始动作共享 `30_000ms` 运行就绪等待上限；它只是终止无响应夹具的 hang guard，不是启动性能目标，夹具必须等继续按钮真正可用后才点击。

HUD 回归继续锁定桌面双锚点、七个至少 `44px` 的结构化操作键、领航详情、开发天气和五级 FPS，并覆盖移动驾驶舱。`1440×900` 现在属于高度 `561–1,079px` 的紧凑桌面战术列，并额外断言展开领航详情完整位于视口内且避开地图和罗盘；高度 `1,080px+` 的完整桌面继续逐个测量操作键标签、状态、copy 与 meta。Node 静态检查锁定 `viewport-fit=cover`、四个常驻值、两个真实 disclosure、折叠内容、四向安全区、触控能力优先和手机单详情/iPad 独立详情。新增合同要求单一 viewport 协调器把 app、visual viewport、DPR、CSS、相机、Renderer、后处理、HUD 与地图绑到同一快照；同页 iPad→手机 resize 必须只保留最后打开详情，工具抽屉/航路、详情/领航胶囊、详情/换挡器及驾驶键必须逐对无重叠。

电影模式回归跨四层锁定 `cameraCinematicContract v4`：桌面与移动入口共享语义，桌面公开 `F`，`C` 的四视角循环不变。F 只保存并接管镜头，绝不读取或改写 P、M、T、Q/E、方向、油门、刹车、跳跃、挡位、速度、路线或得分；P 关闭时玩家持续拥有驾驶权，P 开启时才由领航驾驶，而且电影中所有驾驶开关与输入仍可操作。普通接触只阻止扣除生命并移除已接触障碍，不得产生转向、制动、换挡或跳台规划。跳台 readiness 与支撑平面继续仅由道路和落地资源决定，镜头状态不得参与。致命恢复只能退出镜头并恢复原视角，不能恢复或覆盖任何驾驶偏好。运行时仍验证三套桌面十二镜与三套触屏八镜、硬切/柔切、世界固定与船体挂载、隧道编排、只读 RNG/路线/玩法边界、减少动态稳定构图、`44px+` 入口和无中央遮挡；镜头舒适度与道路可读性仍需真实 WebGL 验收。

电影 HUD 的静态布局合同另锁定 `390px` 边缘监看台、速度/挡位跨三行主卡、得分/成长/生命三张次卡，以及 P/M/F 首行和暂停/全屏第二行。高度 `≤620px` 的非移动样式必须保留同一层级并收紧到 `372px`，不得重新退化成五键单行；专用 Node 合同证明选择器和断点存在，真实尺寸、文字截断与画面遮挡仍由浏览器验收。

真实布局矩阵按输入能力而非只按像素命名：手机竖屏 `320×568 / 360×640 / 390×844 / 402×874 / 440×956` 的两个顶层外壳使用已定义的 `8px` 接缝，手机横屏覆盖 `568×320 / 640×360 / 700×390 / 720×460 / 800×460 / 830×560 / 844×390 / 874×402 / 956×440`；`320×568` 手册还必须在无需滚动时完整显示第一个编号步骤。触控平板另补 `900×700 / 920×700` 顶栏边界，并覆盖竖屏 iPad `768×1024 / 820×1180 / 834×1194 / 1024×1366` 和横屏 `1024×768 / 1180×820 / 1194×834 / 1366×1024`。Magic Keyboard/精细指针共存时仍保留触控驾驶键；相同窄窗或矮窗尺寸的纯 fine-pointer 环境可使用紧凑 HUD，但须保留键鼠驾驶、隐藏虚拟踏板。每个移动用例都要实测四值常驻、折叠按钮焦点与双语 ARIA、手机单详情/iPad 双详情、五个驾驶键和全部操作 `≥44px`、横竖屏与非零安全区收纳、地图展开后的真实 Canvas 尺寸，以及 HUD/详情/地图/驾驶键两两不重叠。`921×561 / 1024×600 / 1280×720` 继续覆盖非触控中高横屏的领航 OFF/ON。Node、普通 iframe 或文档 `scrollWidth` 不能证明 Safari 安全区、动态浏览器栏、旋转或 Magic Keyboard 能力；这些边界必须由真实/等效触控浏览器检查，最终仍需 iPad Safari 实机验收。

移动 UI 回归还必须锁定四组边界。第一，飞行态手势禁用只能命中 Canvas、实时 HUD、详情抽屉和五个驾驶键。第二，常规矮横屏五键实测为 `52–60px`；`568×320 / 640×360` 改用左侧单排五个 `44px` 键、右侧完整换挡范围槽及至少 `80px` 高的可用详情。第三，`900×700 / 920×700` 的航路槽必须在顶栏下方避让五格工具抽屉，宽度达到 `1024px` 后才进入顶线中央。第四，弹层与飞行呈现都要在 visual viewport、旋转、浏览器栏或 DPR 变化后重新断言同快照几何；Node/CSS 不能替代真实移动浏览器或真机验收。

生命/烛光隔离回归要求静态入口保留恰好三个稳定生命槽、三段完整度轨及单条连续烛光轨，并锁定 `3 / 2 / 1 / 0 → healthy / wounded / critical / depleted` 与 `base / responsive / agile / elite` 映射。源码合同分别截取 `updateLifeMeter()` 与 `updateHandlingMeter()`，移动镜像也纳入双向禁止写入名单，并锁定六位小数成长进度。去除注释后的 CSS 会逐规则审计：生命选择器不得包含烛光族，烛光选择器不得包含生命族，任一声明块都不得消费对方变量；紧凑、高对比、强制颜色和减少动态规则同样受检。浏览器还须分别改变生命和烛光，确认只有目标卡的槽、颜色、ARIA 与 data 状态变化。

战术小地图回归要求主/待命画布始终不同，一次恰有一个 `presented` 所有者，完整帧发布成功后才同步提交模式、朝向、威胁、引导、航向读数和无障碍状态。运行时合同还执行验证障碍距离分桶、两次可恢复重试与第三次连续失败升级、成功后清零、页面卸载一次性销毁，以及仅 `modelDebug=1` 暴露的单帧故障注入。响应式夹具按实时 `presented` 画布检查缓冲区、单一航向语义所有者和完整 DOM 状态；静态 Node 通过仍不能替代真实画布的帧保留与交换验收。

道路批次回归另锁定 `19` 个预分配直接批次、active 图块真实可见批次 `≤16`、inactive 父图块为 `0`，以及转弯箭头和出口牌同时显示时只隐藏冗余通用编织引导层。正式浏览器夹具保持 `≤16` 可见预算并要求预留诊断严格为 `19`；已选/候选路线、玩家箭头和地图投影合同继续独立断言。

启动防阻塞回归要求：i18n 变化观察器只重放受影响子树且不改写 `document.title`；加载阶段只能由模块、世界、首段 current/next 道路、所选画质着色器编译/链接诊断、首帧和动画循环的真实完成信号推进，禁止百分比、预计时间和纯计时器进度。首段两条道路不得同步调用完整构建，而要按有界关键任务先后完成并原子显现；运行时须等待两者 ready，再异步编译所选画质 shader、成功呈现首帧并启动动画，之后才原子发布已启用 Start。更新租约路径须证明普通 Continue 只发布已就绪开始页且不解锁音频，`autostart=1` 则不闪现开始页并把 loading 保留到首个玩法帧成功；任意 fatal 都只留下重新加载。世界测试还锁定池根首次真实放置前隐藏、放置后可见；烛光测试锁定所有共享几何的不可变拓扑标记，避免 `modelDebug` 对池副本重复扫描。Node 合同只能证明调度与状态边界，真实页面仍须验证启动期间主线程可响应，并观察从 loading 到可立即点击开始页之间没有等待窗口。

地表单元回归覆盖三层合同。玩法核心用椭圆与矩形足迹的精确 Minkowski 扫掠验证 `300m/s × 45ms` 长帧不会穿过小水坑/雪斑，并覆盖斜角边界/切线、前进/倒向对称、横移、起终点在内、无效/溢出输入、可复用输出和 `2_000` 组确定性随机反向性质。飞船测试锁定不可变碰撞/压力足迹和双尾下洗点。天气测试锁定 `24m` 稳定桶、后方至少 `96m`、移动/桌面前视至少 `1_104m / 1_488m`（在 `300m/s` 下为 `3.68s / 4.96s`）及开局负里程桶隐藏，并要求水花由扫掠覆盖、纵横浸入、压力、速度和着陆能量连续决定：擦边不喷整包水花，左右尾点分别判定，每次接触/每束最多 `12 / 8` 粒。其余覆盖露天/隧道/`0.58m` 下洗边界、`1.2m/s²` 局部上限、横向倍率下限、水位移、波能、雪压实、清沟、左右雪脊及其 `[0,1]` 守恒；`30 / 60 / 120Hz` 的六秒恢复结果必须一致、零增量完全冻结。世界测试另锁定同容量雨雪池的低差异近/中/远前缀、桌面/移动远层 `300 / 210m` 水平半跨度、雨一组/雪两组不变，以及已提交路线控制的 `12–30m` 局部雨幕遮蔽：普通桥下近景可遮、远处户外雨幕必须保留，封闭隧道隐藏可见雨线但原雨池继续推进，相机预览不得提前遮雨。强制 `300m/s` 样本只验证扫掠、铺设和压力边界，不代表正常玩法目标；持续 `60s` 的平均地表减速度不得超过 `0.9m/s²`。渲染合同还锁定雪片高反照率实体覆盖、薄片只接收/体积雪堆投影，以及移动混合地表最多六个参与绘制组。音频测试分别验证水花、雪压实、干雪脆响和更暗的雪泥频谱；雨声测试锁定小雨细节、中雨雨幕、暴雨低频三层，证明原始雨量决定音色、普通桥下保持完整户外雨声、山体/地下隧道分别保留 `18% / 10%` 的低通传导、小雨不超过暴雨的 `16%`、全天气增益和不超过 `0.032`，并在连续切换时保持同一图和零逐帧分配。

路肩雪回归另锁定每桶左右成对、实际 `roadHalf` 布置和零新增绘制组；纯几何用例镜像验证左右擦碰、深入接触强于擦边、`0.30m` 下洗有效，以及 `0.581m`、禁用、无雪、隧道遮蔽和未命中全部隔离。稳定单元还验证雪堆压实/外推保持 `[0,1]`、暂停冻结并在 `30 / 60 / 120Hz` 等时恢复。运行时必须公开本帧雪堆接触、累计互动、受扰雪堆单元、扫掠命中和雪堆粉雪次数；这些自动化只证明合同，雪堆真实降低、外推、压痕与粉雪观感仍需 WebGL 目视。

垂直起飞回归另外锁定手工 `SPACE` 与触控按钮都进入同一个 `jump() → launchVertical()` 权威链。可执行运行时夹具以 AT 2 挡、`150km/h` 分别覆盖平路、`3%` 结构纵坡与 `28%` 实体跳台承载面，三者都必须首次起飞成功、第二次因已离地被拒绝，且不改速度、挡位或变速箱模式。坡面用例还要求初始垂直速度精确等于坡面切向分量加投影后的主动冲量，而 `jumpPlatformFlightActive` 仍为假，确保手动起飞不冒充坡唇腾空或取得跨车道落地权限。玩法核心在 `22 / 30 / 60 / 120Hz` 从精确贴地状态施加按标准重力 `9.81m/s²` 反算的生产跳跃冲量，必须在首帧离地、达到约 `4.6m` 峰值并在约 `1.94s` 后落地；同组用例还要求静止与下降物体继续在 `t=0` 接地，防止修复分离根时破坏正常碰撞。

跳台对向落地另使用一次不可变的目的道路握手统一物理、碰撞与呈现权威。进入含实体跳台的 corridor 就必须请求预热；只有目的 tile 的普通签名同时完成视觉、碰撞与上一成功渲染帧确认，平台承托和坡唇释放才可生效。未就绪检查必须发生在 `grounded`、起飞计数、最后起飞与扫掠状态的任何写入之前，并累计阻止次数与原因。地面预热期间目的 tile 保持 pin，腾空后源/目的双 tile 同时保持；碰撞必须显式消费同一目的 token。安全落地、真实路外坠毁与重开都释放本次握手；对向路线采用无条件拒绝不可见的 landing surface，但几何查询真正 miss 仍必须进入 `off-road-crash`，不得用 readiness 伪造路面。`markPresented()` 只能在真实 presentation frame 成功后提交，因此“对象已创建”不能冒充“玩家已看见道路”。

纵向推进回归锁定三段满油门推进力在同一滚阻、线性阻力和空气阻力下自然平衡于约 `110 / 160 / 280km/h`。松开 W 立即归零踏板额外推力；贴地1挡、零踏板、未制动且未断扭时独立怠速力让干燥平路缓慢趋近约 `8km/h`，从 `16km/h` 起严格归零，S 可完全停住，2/3挡、换挡与腾空无隐藏蠕行。挡位合同覆盖 `70 / 140km/h` 升挡下限、`125 / 190km/h` 安全降挡上限、不安全拒绝不改速度，以及接受换挡的 `0.30s` 断扭/RPM 回落；2挡 `35→70km/h` 与3挡 `90→140km/h` 的平方拖挡曲线必须真实乘入区间推进，使低速高挡无法抵消阻力。运行时夹具证明 T 变速箱与 M 满油门偏好独立形成四种组合：MT 在 M 开/关下都保留 Q/E，AT 将桌面与移动的 Q/E 同时移出布局、辅助技术可见性和指针激活路径，并按 `90 / 70km/h` 与 `140 / 120km/h` 两组滞回自动完成 `1→2→3→2→1`；返回 MT 必须原子恢复两组按钮，多次往返不得重复绑定事件。生成压力回归以真实 `1→2→3` 轨迹在 `30 / 60 / 120Hz` 等渲染分片保持事件顺序、随机种子、里程和固定步长守卫一致。

自动起步时序另在 `120Hz` 锁定 T 自动挡于 `90km/h` 升2挡、`70km/h` 降1挡、`140km/h` 升3挡、`120km/h` 降2挡，以及两组 `20km/h` 防抖区；P 的 `120km/h` 巡航通常停留2挡，无上限满油门则按唯一顺序自动进入3挡。呈现回归执行验证桌面/触控挡位可见 `1→2 / 2→3 / 3→2`、拒绝时可见本地化短原因，且 AT 下全部手动挡位输入隐藏、禁用且不可聚焦，移动选挡器的实测高度与网格行必须实际回收；MT 恢复可见控件但不改变飞行态顺序焦点政策。MT/AT、三挡推荐工作带、当前自动升降挡阈值和按模式区分的 `lugging / stalled` 提示必须在桌面与移动仪表同步。拒绝提示的 `1_600ms` 墙钟租约在到期前保持、到期时只刷新一次；重复失败重置完整时限，暂停不能冻结提示，最后拒绝原因与计数仍保留，持续拖挡/失速状态则必须在红态消失后立即接管仪表。

真实浏览器地表验收应以本地 HTTP 服务分别打开 `?autostart=1&auto=1&seed=2301&quality=high&weather=rain&cloverleafMove=straight&modelDebug=1` 和对应的 `weather=snow`。露天直道先核对空中雨雪同时存在近/中/远景，且积水、雪斑和路肩雪堆已经铺到远方，不能在机头近处整批冒出；诊断的实际地表前视必须达到当前画质合同。随后目视确认擦过水坑边缘只出现局部扰动或单侧少量水滴，深度命中才分别从实际浸水尾点形成小水柱，并继续确认水面压低/摆动、接触点涟漪、V 形尾流，或雪层压暗/降低、双沟、侧雪脊与粉雪。回流/回填由可重复的单元时间步和受扰单元诊断验证，不能要求高速相机持续追踪已离开池的同一地块。诊断必须显示相应接触序列、局部水柱/擦边无水花/发射粒子、受扰单元及效果计数增长，减速度和横向倍率保持合同范围；普通高架桥下接触恢复 `dry`，近处雨线可被桥面遮住但桥外远景雨幕与户外雨声必须继续，封闭隧道隐藏可见雨线却仍保留压暗的非零雨声，覆盖区违规计数保持 `0`。音频验收须在玩家手势解锁后分别辨认清水、干雪、雪泥，并依次试听降雨、暴雨、雷暴和冰雹过渡：小雨应柔和，中雨连续但不盖住配乐，暴雨以低沉重量增加而不是高频嘶声；Node、截图或诊断数字都不能替代实际 WebGL 运动与听感检查。

PC Ultra 回归覆盖阴影能力回退、Low/Medium 原值、High-only 隔离、后处理分配/释放、显式 Bloom 白名单、飞船/世界可逆材质及无玩法权威。地貌回归同时锁定浮动原点 uniform、绝对世界坐标恢复和非相机锁定诊断；浏览器夹具在飞船前进后核对程序坐标与实体 world cell 使用同一原点，并只在强制过渡中点要求生态交错、混合单元与边界对为正，纯地貌起点允许三者为零。地图航向断言在同一暂停代次内冻结已呈现地图诊断和唯一无障碍 DOM owner，禁止拿重开前的诊断与重开后的实时节点交叉比较。`browser/Neon_Autopilot_V23_HighSpeed_DroneHeat.postprocessing-pixel-test.html` 在同一 WebGL2 renderer、场景、相机、目标格式与 4× MSAA 输入下读取完整 RGBA，要求旧执行路径、去冗余路径及重复渲染之间的通道差异严格为零。道路集成回归锁定 production merged batch 的 apron/lens 与 junction shader range 都为 `0`，route edge shell 是唯一可见顶面；分流/合流按 `0.25m` 审计 `48` 个 incident pair、合计 `9,292` 个 station，验证 sibling shell 同高、零纵坡、零横倾，以及 direct/loop 的 `164m / 164m`、`164m / 136m` 喉部和后续 `120m` C2 横倾过渡。几何合同还覆盖 `collectorLength=354m`、`collectorOffset=240m`、`directRadius=400m`、下层 collector 的 `grade-limited-c2`、`13.5m` flyover 和 `≤6%` 纵坡；隧道回归锁定连续 indexed shell、`≤2m` station、`≤0.005m` 弦误差、portal/rib 共站、完整屋顶最小净空 `0.608719m` 和 `16` 个洞门框最小 sibling 路壳间隙 `1.546623m`，两项都须保持 `≥0.25m`。玩法单元与运行时合同通过整帧 swept vertical contact 验证高速下不穿道路或顶棚。Node/static 只能证明合同、有限数值、封闭拓扑和调度边界，不能作为浏览器视觉证明；真实 WebGL 仍须连续驶过合流、分流和上下层衔接，检查曲率、横倾、裂缝、闪烁、Z-fighting、黑屏、阴影伪影、穿模与切档资源稳定性。

`SKY_TERRAIN_ART_CONTRACT` v2 的实构回归使用真实 Three.js 分别构建 High 与 mobile 的六份纯境模板和六份生态交错模板，共 `24` 份几何。每份装饰后缀都独立焊接顶点并要求每条边恰好使用两次，同时锁定 `0` 边界边、`0` 非流形边、`0` 退化三角、`0` 重复三角和 `0` 非有限顶点；中央 `±10m` 必须保持安静留白。回归还验证十二个预建实例层、自然介电 `metalness=0`、运行帧不重建几何，以及 terrainHeight、隧道裁切、道路碰撞、玩法 RNG 和池容量权威不变。

保底互通桥回归直接执行运行时代码中的真实 Three.js 创建块，要求 `SKY_ROUTE_SCENERY_CONTRACT` v2 的 `16` 层 × `32` 边桥墩与 `16` 点 × `6` 轴环横梁均为封闭流形，局部包络不超过旧矩阵所依赖的边界，暖石材质满足 `metalness≤0.03 / roughness≥0.78 / clearcoat≤0.04 / emissive=0`，且固定 `4 / 2` 实例容量不变。独立销毁夹具把同一接触阴影同时放入活跃实体和含重复引用的池，连续调用两次卸载函数，证明桥梁、共享阴影资源及每份克隆材质都按对象身份只释放一次、池与实体引用被清空；源码门禁另禁止旧障碍/拾取材质在生产启动段恢复批量分配。

High 远山回归逐个构建六境真实 Three 网格，要求每境一个不透明 `MeshStandardMaterial`、三条有径向厚度与垂直起伏的 relief range、`96` 段、`1_728` 三角形、零实例/自发光/自有灯光，并验证 `18` 条量化轮廓签名全部不同。相同配置必须产生完全相同的 position buffer；更新与 Low→High 切档不得重建或改写它。每境唯一 morph target 必须直接引用下一境同拓扑的 position/normal buffer，独立 `neonV23NextHorizonColor` 属性必须引用下一境 color buffer；测试还模拟 Three 着色器 chunk，证明 WebGL1/2 共用的颜色 uniform 注入生效且与共享境界 blend 精确同步。常态和过渡都恰好一个真实绘制组。创建、更新与切档前后会完整快照相机位置、四元数、缩放、up、FOV、aspect、near/far、zoom 和投影矩阵，同时把相机 Y 抬高后证明远山世界 Y 仍为 `0`。Node 能证明几何、材质、预算、确定性和相机只读合同，但山坡真实受光、大气遮挡与运动视差仍须由 WebGL 目视验收。

地下罩体回归现在执行真实 production `BufferGeometry`，分别以桌面 `2.5m` 与移动 `5m` 道路采样构建全部八段隧道。道路肩部 `0.22m`、墙中心/厚度 `0.49m / 0.54m` 与屋顶外扩 `0.84m` 必须解析为墙脚缝隙 `0`、侵入 `0`、墙顶缺口 `0` 和屋顶外搭 `0.08m`；每个道路壳体 row 必须并入罩体 station，罩体 station `≤2m`、弦误差 `≤0.005m`。地下门框与道路接点至少分离 `0.02m`。地貌裁切第 2 版不得再出现固定最小半径，标准环道严格推导为 `5.60m`，使裁切深入内墙 `0.18m`、距外墙仍有 `0.36m`。浏览器诊断必须发布共享接点、缝隙、侵入、道路折线对齐修正、墙顶缺口与门框净距；这些数值只能证明结构合同，最终仍需在 `south + left` 地下路线连续查看入口、弯道、坡变和出口有无闪缝、Z-fighting 或穿模。

真实速度回归锁定新局从 `0` 起步、松开 W 立即归零踏板额外推力、1挡怠速蠕行是力平衡而非速度托底、滚阻与气阻在蠕行范围外持续降速、S 可精确刹停并压住怠速、P 领航普通道路巡航稳定在约 `120km/h`，而 P 关闭、M 开启时持续满油门且不受该参考限制，以及三推进级在干地自然平衡于约 `110 / 160 / 280km/h` 和 `7.5m/s²` 行车制动。独立单位回归仍锁定 `105km/h = 29.1667m/s`，并要求 `1 / 2 / 3 / 4 / 10 / 30 / 60fps` 的可见运行在十秒均前进 `291.6667m`；每个有限墙钟帧必须完整消费为不超过 `45ms` 的子步，隐藏页恢复后不得追赶后台时段。速度拖影从 `70km/h` 渐入并在 `160km/h` 达到完整强度；横向运动与速度 FOV 同样在 `160km/h` 饱和，3 挡不会额外制造速度错觉。中央动态路线亮线、光点和目标环保持删除，世界固定米制短标、道路箭头、出口牌与路线图继续提供参照。

相机视角回归必须同时覆盖俯视与车头。运行时合同锁定俯视的 `20–22.5m / 11–13.5m / 20–30m / 70°` 战术构图、零相机合成巡航与 `30%` 飞船合成巡航/基础悬浮，以及车头的 `2.05m` 眼位、`1.32m` 前置、`1.18m` 鼻尖、`0.14m` 净距、`1.25m` 视线高、`46–76m` 前视、`65°` 和权威 `state.lateral`。基础触屏光学风格继续校验真实触控与纵横比双门、`0.78 → 0.46` 混合、追尾高度 `×1.06`、近距前视 `≥×0.78`、俯视 `×1.18 / ×1.12` 与 `26m` 包络，以及开放道路 `4° / 5° / 5° / 7°`、有顶道路 `4°`、基础目标 `84°` 的边界。另由 `driving-camera-framing.test.js` 验证普通追尾/近距后置补偿覆盖窄 fine-pointer 桌面和 HUD 拥挤横屏：纵横比与 HUD 高度权重取较大值，净高 `50% → 30%` 介入至满补偿，保留 `×1.32 / ×1.60`、地下 `9.2m` 和 `30/60/120Hz` 等时阻尼。最终追尾/近距/俯视八角投影保障采用常规 `84°`、必要时 `100°` 上限；极限姿态或不可能的 HUD 缝必须区分 `fits` 与 `viewportFits / hudOccluded / fallbackActive`，不能将画布兜底冒充 HUD 通过。Film、Hood、主动自由观察及回中阶段继续排除，模型与最终镜位不可被安全层移动或缩放。浏览器须跨触控/非触控、横竖屏、HUD 展开/收起以及道路转弯、坡度、腾空、进出隧道连续采样，证明安全矩形与净空成立、战术构图可读、车头不随船体合成摇摆，且画质缓冲、飞船、碰撞与玩法快照不变。`4:3` iPad 基础触屏增益虽小，仍须通过实际 HUD 检查；单帧截图或只循环视角不能代替连续验收。

`hood-collision-guide-v3` 回归从飞船模块字面量 `colliderHalf={0.72,0.58,1.36}` 推导并断言真实碰撞尺寸严格保持 `1.44 × 1.16 × 2.72m`，运行时不得读取可见船体 `geometry/scale/userData` 或 `Box3` 重算。呈现合同只允许沿已提交而非候选 `PathPlan` 输出四个短角标与两条下侧导轨，禁止完整 wireframe、远端门、中心轴和中央卡片；角标内部与驾驶消失点必须透明。桌面只显示位于视口下缘且避开输入的宽度读数，移动端必须隐藏。威胁夹具需证明探针只读取已提交路线和复制的权威运动快照，并受有限 `2.5s` 警戒窗及固定迭代/采样预算约束；分别覆盖正常贴地、窗内未来跳台 lip 起飞、已经活动腾空、地面相交、顶棚相交和安全穿越，并要求碰撞体在横向、高度及时间上同时重叠后才进入 `2.5s` TTC 琥珀警戒或 `1.2s` TTC 红色危险。静态合同必须排除候选路线、地图仅按距离得出的最近障碍，以及长跨度、高密度物理扫掠；调用前后的跳跃/腾空、姿态、速度、路线、横向位置、生命、碰撞、控制和物理快照必须完全不变。量规只在运行中车头视角显示，并在相机更新后、呈现前每帧恰好投影一次；无效路线基、探针、地面/顶棚采样、腾空推演或投影必须安全降级或隐藏且不得钳在屏幕边缘。源码和 CSS 还必须证明无动画。真实浏览器需连续经过直道、弯道、隧道、跳台起飞和腾空段，确认六条短标随已提交路线移动、亮暗场可读、中心道路无遮挡、上下表面威胁只在真实 TTC 窗口增强、下缘读数不压住 HUD/驾驶键，且移动端没有该读数。

推进与转向运行时回归由 `contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.propulsion-runtime.test.js` 锁定。纵向速度只能由同一步解析器把区间平均踏板推进、独立1挡怠速力、滚阻、线性阻力、空气阻力、地表载荷、坡度与制动合并积分；松油门立即切掉额外踏板推力，制动则连怠速一起切断，RPM 与 `exhaustEnergyNormalized` 仍按权威余转自然衰减。回归覆盖 G1 怠速起步、约 `8km/h` 平衡、`16km/h` 切断、S 停车保持、G2/G3/换挡/腾空断开、滑行范围外单调减速、三挡 `110 / 160 / 280km/h` 满油门干地自然平衡、`0.30s` 断扭、不安全换挡拒绝不改速度、2/3挡低速拖挡、MT/AT × M 四组合、AT 按 `90 / 70km/h` 与 `140 / 120km/h` 自动覆盖三挡、P 领航约 `120km/h` 巡航、P 关闭时 M 持续满油门且不调用目标速度控制器、`7.5m/s²` 行车制动、不同帧率合理容差和拾取烛光不直接奖励速度。飞船回归另锁定松油首帧主焰长度与亮度保留超过 `97%`、余焰随 core spool 单调衰减而实际推力保持零。手动与自动领航继续共享渐进转向执行器、规划 rollout、门户预测和不超过 `45ms` 的有界子步；暂停冻结完整反馈状态。

飞船重构回归还锁定 `dawn-pilgrim-manta-reliquary-v2` 的五瓣/三褶/双层披风、双眼灵魂面具、零现代装甲面板、零可见涡轮及 `70%` 安静表面合同。High/Mobile 的全部主视觉网格必须保持闭合、有限、无边界边、无非流形边和无退化面；健康/受伤/危急三态的真实可见 Box3 均须落入同一保守覆顶包络。局部装配测试验证披风根、织物层、围巾、主/侧光孔的声明净空，并在怠速、满推、满舵与制动状态下读取真实顶点，要求主焰、内焰、左右侧焰和左右反推焰的根平面保持固定、不得越过喷口向船体内伸，尾端只能沿喷射方向延长。固定碰撞体、三生命形态、成长换色、差动尾焰、反推、三环、四尾带、速度线、护盾、俯视环、质量切换和幂等释放接口均须继续存在。

烛光成长回归由玩法核心、飞船、音频和运行时测试共同锁定。音频测试固定 `V23-audio-13`、`V23-effect-assets-1` 与 `authoritative-core-drive-v5`，验证九个内联载荷逐字节等同 OGG、`file://` 零 `fetch`、发布代号请求、损坏 HTTP 200 回退、恢复/解码竞态、`1_000ms` TTL，以及隐藏/生命周期停止已排期雷声；两份精确 OGG 都失败前不得合成。

配乐回归锁定 `V23-music-library-4`、`V23-music-rhythm-4` 与 `V23-music-player-8`。播放器要求试听复用正式飞行增益；`modelDebug=1` 必须读取玩家已有模式/曲目作为初态，但写入为 no-op。合成布局夹具保持零真实事件计数，只能作为“不解锁”的负向检查，不能冒充听感证据。

- 自动领航 Liquid Glass 的静态合同锁定：七键指令舱在启动/继续/再次启航时仅以 `16–26px / 420ms` 的自身 transform 上滑，减少动态直接终态，且 HUD/地图/fixed 定位祖先不参与变换；`.auto-panel` 是唯一 backdrop 层且状态单元不嵌套模糊；四个命名动画只声明 `opacity / transform`；风险刻度为 `30% / 58% / 82%`；四级风险与横向权威、纵向权威、制动、威胁语义轨的数据状态完整；桌面/iPad 展开完整面板，手机使用 `9px / 10px` 静态摘要；减少透明度、无 backdrop 与强制颜色回退存在。源码断言只证明选择器、声明和 DOM 状态合同，不能证明浏览器实际使用合成器、动画帧稳定、玻璃光学效果或实机 GPU 成本；弹出时序、终态收敛、遮挡与触控布局需由浏览器矩阵验证，性能和视觉质量仍需真机验收。

无敌结界回归锁定深冻结的 `dawn-starward` 外观合同、权威时间纯派生、`0.18s` 单次展开和 `0.34s` 连续收束，并采样起始/稳定/尾端/归零确保透明度、缩放与相位有限且尾端不亮切。飞船真实构造必须包含一个不进 Bloom 的封闭 FrontSide 雾适配场、三条窄 Bloom 断口星轨和一个五实例锚点批次，所有对象保持 collision-independent、零灯光和零阴影；Medium 只显示两轨三锚。Node 另要求场材质独立克隆 `fogColor / fogDensity / fogNear / fogFar`，并锁定启动期“零透明公开 → 编译 → finally 隐藏”的顺序。`browser/Neon_Autopilot_V23_HighSpeed_DroneHeat.collision-render-test.html` 使用真实 Three/WebGL 分别激活 Low / Medium / High 的首个护盾帧，要求预热后程序数不增长、全部程序可运行、上下文未丢失且 GL 错误队列为空。运行时合同继续要求首次受击只调用飞船模块更新接口，不得把 Group 当 Mesh，车头隐藏世界罩并复用真实碰撞括线，俯视复用方向环，暂停与减少动态冻结相位，桌面/手机/车头进度读取同一 `invincibleTimer / 1.32s`。无障碍回归锁定静默倒计时、双端环形进度、减少动态零循环和强制颜色系统边界。Node 只静态确认浏览器夹具随版本交付；雾、Bloom、透明排序和各视角遮挡仍需真实 WebGL 执行与目视验收。

iPad 专项回归不再只验证 `≥44px`：静态合同精确锁定驾驶键 `80–88px`、直接操作与工具键 `56–60px`、横屏单排/竖屏 `2 × 2` 以及航路槽固定 `64px` 高度。横屏槽必须与安全区顶栏同轨、水平居中并与左右 HUD 各留至少 `12px`，竖屏槽继续在顶栏下方至少 `64px`；浏览器夹具会在普通航路、长中文、长英文、限速提示、工具抽屉打开与关闭之间逐次比较航路槽 `x / y / width / height`，并要求三段文字均为单行省略。双轴 `≥700px` 触控视口还要求中央档位器与四个底排驾驶键共用真实安全底边、水平中心误差 `≤1px`，并在 MT / AT / Film 与 `1024×768 / 1180×820 / 1194×834 / 1366×1024` 横竖旋转后保持底边与零重叠；`1180×820 CSS × DPR 2` 明确对应用户截图的 `2360×1640` 设备像素。八种 iPad 视口仍需同时通过安全区收纳和 HUD/详情/地图/驾驶键零重叠，手机矮横屏继续使用原有分舱。静态通过不等于真实 Safari 布局验收。

高挡低速回归同时锁定物理与呈现：3挡在 `140 / 115 / 100 / 90 / 7km/h` 的线性 RPM 耦合必须为 `1 / 0.5 / 0.2 / 0 / 0`，平方扭矩可用度为 `1 / 0.25 / 0.04 / 0 / 0`；基础 `12_000 RPM` 与成长 `15_000 RPM` 都要在满油门完全失速时单调回落，零推力和真实阻力把速度降至 `0`，且 `30 / 60 / 120Hz` 结果一致，1挡静止起步不受影响。运行时夹具分别覆盖部分拖挡与完全失速、建议恢复挡、`Q×N` 文案、中央警告优先级、桌面/移动数据状态、live-region 去抖、音频负载与尾焰负载透传。音频测试还要求不新增节点或事件，飞船测试要求普通动态有界喘振、降低动态只保留静态衰减；Node 证明状态和数值合同，最终显著性与听感仍需真实 Chrome 目视/试听。

## English

Ship tunnel-light regressions separately cover resident-road sampling, runtime forwarding, and production material extensions. Route/profile mismatches, hidden visuals, and disposal must clear the probe, portal transitions remain continuous, and Low/Medium do not enumerate High fixtures. Opaque and damaged variants share stable PBR diffuse uniforms without changing emission or highlights. Browser checks must still inspect the craft's color and silhouette under lamps; shader text checks do not establish visual quality.

Road-edge camera regressions execute the production follow damping, FOV, roll, and framing sequence over consecutive frames, including both stationary edges, lateral traversals, and different frame rates. With road, hull, and HUD fixed, orientation and FOV must converge without periodic jumps even when `fits=true`. Separate checks ensure the final safety correction cannot contaminate the next follow frame and roll cannot accumulate.

Small-window and load regressions execute production HUD, safe-rectangle, and camera functions. A stable `600`-frame run counts redundant DOM/translation writes; hide/reveal, language, rapid input, and timer expiry remain immediate. Camera matrices project the real Three ship envelope across sizes, grades, banking, and flight. Structure tests retain batches/materials and verify moving bounds against actual vertices with independent camera/shadow frusta; texture revisions cover same-count replacement and late disposal callbacks. Offline call timing is not game FPS. `browser/*hud-layout-test.html` is a standalone layout fixture. The production LAN server intentionally excludes `tests/` and disallows iframes, so live acceptance resizes the production page and reads its debug DOM without weakening the server boundary.

`integration/Neon_Autopilot_V23_HighSpeed_DroneHeat.topology-lifecycle.test.js` owns the long-run road-topology regression. It continues beyond an explicit 20 km checkpoint across 100 physical tiles and fans out straight/right/left candidates at every tile. Current/high-water registries, clearance and visible-edge scans, aggregate token characters, and a `55`-character maximum token stay bounded, while every step validates the current road, signed Film look-behind, forward sampling, map-visible edges, and full-footprint landing support. A final outside-window recovery source proves that only its adjacent destination survives, never the farther future chain.

Tall-gear low-speed regression locks physics and presentation together. In Gear 3 at `140 / 115 / 100 / 90 / 7km/h`, linear RPM coupling must be `1 / 0.5 / 0.2 / 0 / 0` and squared torque availability `1 / 0.25 / 0.04 / 0 / 0`. Both the base `12_000 RPM` and grown `15_000 RPM` core must fall monotonically under full-throttle complete stall; zero thrust plus real resistance brings speed to `0` with matching `30 / 60 / 120Hz` outcomes, while a standing Gear-1 launch remains intact. Runtime fixtures separately cover partial lugging and complete stall, the recommended recovery gear, `Q×N` copy, central-warning priority, desktop/touch data states, live-region deduplication, and audio/exhaust load propagation. Audio coverage also forbids new nodes or events; ship coverage requires bounded normal-motion flutter and static attenuation only under Reduced Motion. Node proves state and numeric contracts, while final salience and sound still require real-Chrome visual/listening acceptance.

Windows-launch regression in `server/windows-local-server.test.mjs` locks the complete boot chain: ASCII, BOM-free, CRLF `Start-V23-Windows.cmd` plus its Chinese-name alias; a UTF-8-BOM, CRLF Windows PowerShell 5.1 server; one visible finish path for every exit code; TEMP diagnostics; browser fallback; and schema-2 byte coverage for the main HTML, both CMD files, the PS1, and every runtime asset. A Node pass on a non-Windows host proves only static bytes and source contracts; target-Windows smoke testing must still exercise Windows PowerShell 5.1, cmd.exe, the default browser, and real loopback requests.

The airborne union-support matrix traverses all `256` continuous PathPlan seams across four entries and three movement families, and requires a complete craft-length hit with `connected-road-seam` at the centre plus `±0.5m / ±1.359m` around representative same-tile and cross-tile seams. An inactive next tile and a centre beyond a real endpoint must still return null so the repair cannot become ghost pavement. A `3.8m` off-centre merged-fork sample additionally pins current-plan precedence so sub-micrometre projection noise cannot adopt the selectable sibling.

- `unit/`: single-module logic and asset contracts.
- `integration/`: road, map, spawning, and cross-module composition.
- `contracts/`: entry, runtime, style, update-notice, and guide synchronization.
- `browser/`: real-page, WebGL, and responsive fixtures; a Node pass cannot replace visual acceptance here.

Run the unified gate from the project root with `node tools/verify-node.mjs`.

Cross-batch scenery-clearance acceptance is owned by `integration/Neon_Autopilot_V23_HighSpeed_DroneHeat.scenery-clearance.test.js`, using a fixed seed and real production geometry/APIs. It jointly locks zero road-capsule penetration across all six realms at High `16` and Mobile `10`, zero non-semantic floating or burial for visible world roots, real companion surface spacing, finite absolute coordinates and Film `0.35m` resolution margin for every visible route-structure blocker, millimetre road contact for water/snow with flat-bottom snow banks, zero non-mounting self-intersections across all `18` creature families, plus the Film cylinder pure-input matrix and caller-owned output reuse. Conservative cylinders for real pier-capital-crossbeam attachment seams may overlap; tests prove that each cylinder contains its actual transformed AABB corners and remains lens-resolvable instead of incorrectly requiring structural components to be disjoint. Transparent effects are exempt only through an exact production-name whitelist, never a broad type or material allowance.

Spawn regression uses a `600s` parked run plus low-speed stepping to prove that obstacle, traffic, and pickup clocks advance only with real travel; obstacle-factory coverage also locks available-family capacity across acquire/release. Runtime contracts require `48 / 24` roots per static/dynamic family, an atomic three-root event preflight, independent late-fork and opposing-copy guards, and all three capacity-suppression diagnostics so pool exhaustion cannot terminate a long session again.

Bilingual regression in `unit/Neon_Autopilot_V23_HighSpeed_DroneHeat.i18n.test.js` locks isomorphic catalogs, the Simplified-Chinese default, query/safe-storage precedence, persisted switching, dynamic-node replay, static-entry coverage, zero Han in English, and zero English UI terminology in Chinese. Standalone hardware tokens `A / D / W / SPACE / S / M` must survive both languages and round-trip switching unchanged, while map split/merge remains translated through explicit semantic keys. The launch contract additionally requires six compact dialog controls, none in the HUD, each retaining a `44 × 44px` actual touch box around an approximately `34 × 28px` visible pill. The real-browser matrix must open both `?lang=zh-CN` and `?lang=en` at `320 × 568` portrait, `844 × 390` short landscape, and desktop sizes, then inspect visible and accessible language purity, live switching inside dialogs, dynamic HUD/map updates after launch, every leaf boundary, and horizontal overflow. Document `scrollWidth` alone is not sufficient evidence.

Fullscreen unit regression requires the launch deck upper-right utility and HUD fullscreen control to consume one whole-page state. Initialization, language updates, and `fullscreenchange` may not request fullscreen automatically; only explicit player activation may enter or exit. Both controls synchronize accessible names, `aria-pressed`, state copy, and capability disabling, while fullscreen can neither acquire nor advance the launch action's `ready / committing / commit`. The browser fixture proves only the launch utility's `44px` target, containment, and non-overlap; trusted fullscreen gestures still require direct-page acceptance.

`contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.launch-screen.test.js` locks the sole initial loading surface, a non-live shell, the sole polite live stage status, an unfocusable `role="note" aria-live="off"` tip, the initially `hidden + inert` launch surface, a natively disabled Start, and the five percentage-free Modules / World / Routes / Graphics / First frame stages. It also retains the sole modal root, stage/card/briefing/configuration/action order, decorative `aria-hidden` boundaries, native weather and quality controls, responsive breakpoints, reduced motion, and the `launch / pause / gameover + ready / committing` hooks. I18n regression locks `tipLabel` plus six stable bilingual steering / view / ramps / candlelight / damage / weather IDs, with no percentage or ETA. Source-order contracts must prove tips change only on strict real-stage progress, repeated stages do not change them, no timer or gameplay RNG is used, readiness is never written, and normal publication remains Enable Start → Reveal launch → Hide loading. Update notice stays above loading and fatal recovery above both. The launch deck upper-right fullscreen utility stays outside the launch action and cannot alter pause or debrief actions. The pause contract additionally locks the pause-only Restart Run action, the `R` shortcut, an independent secondary `alertdialog`, default Cancel focus, keyboard containment, and Confirm as the only path into the sole `resetGame()` authority; Cancel and `Esc` must preserve the paused run and its progress. Real-page acceptance must also prove Start is already actionable the first time launch becomes visible, the launch action and fullscreen utility are each at least `44px`, confirmed new-run state, withdrawal after the first successful frame, and focus return to the canvas. The browser matrix additionally verifies no tip-card overflow in narrow-phone portrait, short landscape, and iPad orientations, instant reduced-motion replacement, and visible forced-colors category/system boundaries; existing double-activation, commit-locking, mode-copy, and focus-wrap coverage remains.

Flight-focus regression in `accessibility-smoke.test.js` enumerates `11` desktop-HUD, `12` mobile-HUD, and `5` touch-driving buttons exactly, requiring semantic controls with `data-flight-deck-control + tabindex="-1"`; launch, pause, debrief, manual, music, confirmation, update, and recovery controls must not inherit that marker. Runtime contracts lock modal guards ahead of the live gate, suppression of in-flight `Tab / Enter / NumpadEnter`, browser-default cancellation followed by normal driving for all arrows and `SPACE`, no target-level keyboard listeners on hold controls, and Canvas return after library close or an anomalous `focusin`. The layout fixture also checks `28` unique controls, their `11 + 12 + 5` ownership, sequential-focus exclusion, and Canvas focus after closing the library during flight. Synthetic events cannot prove that a browser-native click did not occur, so final acceptance still requires real Enter, Space, arrow, and Tab input in a real browser.

Invulnerability-ward regression locks the deeply frozen `dawn-starward` presentation contract, pure derivation from authoritative remaining time, one `0.18s` formation, and a continuous `0.34s` tail. Start/steady/tail/zero samples keep opacity, scale, and phase finite and prevent a bright cut at expiry. A real ship build must contain one closed FrontSide fog-aware field excluded from Bloom, three narrow Bloom rear-gapped orbits, and one five-instance anchor batch; every object remains collision-independent with zero lights and shadows, while Medium shows only two orbits and three anchors. Node additionally requires material-owned clones of `fogColor / fogDensity / fogNear / fogFar` and locks the startup order of zero-opacity exposure, compilation, and `finally` reset. `browser/Neon_Autopilot_V23_HighSpeed_DroneHeat.collision-render-test.html` activates the first Low, Medium, and High ward frame through real Three/WebGL and requires no post-prewarm program growth, all programs runnable, an active context, and an empty GL error queue. Runtime contracts retain the first-hit ship API, hood collision brackets, top-view halo, paused/Reduced Motion phase, and shared `invincibleTimer / 1.32s` progress. Accessibility locks the quiet countdown, both circular meters, zero reduced-motion loops, and system-colour fallbacks. Node only statically proves that the browser fixture ships with the release; fog, Bloom, transparent sorting, and per-view occlusion still require real-WebGL execution and visual acceptance.

`contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.guide-sync.test.js` separately owns manual synchronization. Version `18` must contain exactly `43` unique player-facing feature IDs, exclude `weather-manual-test` from the ledger, and keep the five decorative World previews on separate hooks. Static and runtime-source assertions lock Essential as the default `16`-item scope, Complete as all `43` items from the same mounted ledger, the open-time read-only snapshot of phase/lateral and longitudinal authority/speed/lives and candlelight/braking/next action, its compact phone presentation, the five-step 60-second quickstart, the four-state authority matrix where P takes priority and M stores the full-throttle preference, wrapping Previous/Next chapter controls, scroll reset on chapter change, and Close/Return/`Esc` overlay withdrawal with focus restored to the actual opener. CSS contracts separately cover one-row phone chapters outside short landscape, the vertical short-landscape rail, single-column content, hidden redundant phone Return, the small-portrait-iPad full-height shell and `180px` rail, the sole scroller, and visible control targets. The layout fixture now contains dedicated v18 assertions: Essential shows `16` items and hides `27` references, Complete shows all `43`, the read-only context exposes `6` fields, responsive phone chapter orientation and compact context hold, redundant phone Return is hidden, Previous/Next wrap in both directions, every chapter's final item is reachable, `.guide-panels` remains the sole scroller, player-guidance body copy measures at least `12px`, and visible controls measure at least `44 × 44px`. Its iPad cases include portrait `768×1024 / 820×1180 / 834×1194 / 1024×1366` and landscape `1024×768 / 1180×820 / 1194×834 / 1366×1024`. Static contracts prove only that the assertions exist and the browser fixture must run separately; that fixture still cannot replace real iPad Safari acceptance for safe areas, dynamic browser chrome, rotation, or touch hardware.

The startup update decision and launch action share a `30_000ms` runtime-readiness ceiling. This is only a hang guard for an unresponsive fixture, not a startup-performance target; the fixture must wait until Continue is actually available before activating it.

HUD regressions retain split desktop anchors, structured `44px+` actions, Autopilot detail, and the mobile cockpit. `1440×900` now exercises the compact `561–1,079px` desktop tactical rail and requires expanded Autopilot detail to remain wholly in-view and clear of the map and compass; the full command grid starts at `1,080px` high. The new contract binds app/visual viewport/DPR/CSS/camera/Renderer/post-processing/HUD/map sizing to one coordinator snapshot. The fixture also performs a same-document iPad→phone resize and requires last-open detail retention, plus explicit no-overlap checks for utility/route, detail/Autopilot capsule, detail/transmission, and detail/driving targets.

Film-mode regression locks `cameraCinematicContract v4` across four layers. Desktop and mobile entries share semantics, desktop exposes `F`, and the `C` four-view cycle is unchanged. F saves and owns only the lens; it never reads or writes P, M, T, Q/E, steering, throttle, braking, jumping, gear, speed, route, or score. With P off the player remains in control; with P on Autopilot remains the driver, and every driving toggle and input stays operable during Film. Ordinary contact only prevents life loss and retires the contacted obstacle; it cannot steer, brake, shift, or plan a ramp. Platform readiness and support remain road/resource facts with no camera dependency. Fatal recovery may exit Film and restore the prior view only, never a driving preference. Runtime coverage retains three twelve-shot desktop reels and three eight-shot touch reels, cut/blend policy, absolute-world and hull-mounted shots, tunnel edits, read-only RNG/route/gameplay boundaries, Reduced Motion composition, `44px+` entries, and no centre occlusion; comfort and road readability still require real WebGL acceptance.

The Film HUD static layout contract separately locks a `390px` edge monitor, a three-row speed/gear hero, three score/growth/lives cards, and P/M/F above Pause/Fullscreen. Non-mobile styling at `620px` height or below must retain that hierarchy at `372px` instead of reverting to a five-control strip. The dedicated Node contract proves the selector and breakpoint structure; real dimensions, truncation, and world occlusion still require browser acceptance.

The capability-specific matrix covers phone portrait `320×568 / 360×640 / 390×844 / 402×874 / 440×956`, phone landscape `568×320 / 640×360 / 700×390 / 720×460 / 800×460 / 830×560 / 844×390 / 874×402 / 956×440`, compact tablets `900×700 / 920×700`, and the existing eight touch-iPad orientations. At `320×568`, the manual must show its first numbered step without scrolling. Magic Keyboard/fine-pointer coexistence retains touch driving controls; a pure fine-pointer environment may use the compact HUD at the same narrow or short geometry while retaining keyboard/mouse driving and hiding virtual pedals. Every mobile case measures four persistent values, disclosure focus and bilingual ARIA, one phone detail versus two independent iPad details, all controls at `44px+`, safe-area containment, real Canvas size after map reveal, and pairwise non-overlap among HUD, details, map, transmission, and driving controls. Non-touch `921×561 / 1024×600 / 1280×720` retains the medium-height Autopilot OFF/ON matrix. Node, an ordinary iframe, or document `scrollWidth` cannot prove Safari safe areas, dynamic browser chrome, rotation, or Magic Keyboard capability; those boundaries need a real/equivalent touch browser and final iPad Safari acceptance.

Mobile UI regression also locks `568×320 / 640×360` to a single row of five `44px` controls, a complete right-side transmission range bay, and usable `80px+` details. Normal short landscapes retain `52–60px` targets. At `900×700 / 920×700`, guidance stays below the top command rail; only `1024px+` landscape shares the top line. Every visible HUD/detail/control box remains safe-area-contained and mutually clear, while viewport, rotation, browser-chrome, and DPR changes reassert the shared snapshot. Native long-press and real safe-area behavior still require equivalent mobile-browser or device acceptance.

Lives/candle isolation regression requires exactly three stable life slots, a three-segment integrity rail, and one continuous candle rail while locking the `3 / 2 / 1 / 0 → healthy / wounded / critical / depleted` and `base / responsive / agile / elite` mappings. Source contracts extract `updateLifeMeter()` and `updateHandlingMeter()` separately, include mobile mirrors in both cross-write blacklists, and pin six-decimal growth progress. After removing comments, CSS is audited rule by rule: life selectors cannot include the candle family, candle selectors cannot include the life family, and no declaration block may consume the other family's variables; compact, high-contrast, forced-color, and reduced-motion rules are covered too. The browser must change lives and candlelight independently and prove that only the target card's slots, color, ARIA, and data state change.

Tactical-minimap regressions require distinct primary and standby canvases with exactly one `presented` owner, and commit mode, orientation, threat, guidance, heading readout, and accessible status only after a complete frame publishes successfully. Runtime contracts also execute obstacle-distance bucketing, two recoverable retries with escalation on the third consecutive failure, reset after recovery, idempotent unload disposal, and the single-frame fault hook exposed only by `modelDebug=1`. The responsive fixture follows the live `presented` canvas when checking buffers, the single semantic heading owner, and complete DOM state; a static Node pass still cannot replace real-canvas frame-retention and swap acceptance.

Road-batch regressions separately lock `19` preallocated direct batches, `≤16` actually visible batches per active tile, `0` for an inactive parent tile, and suppression of only the redundant generic braided guide while exact turn arrows and an exit placard are both shown. The formal browser fixture retains the `≤16` visible budget and requires the reserved diagnostic to equal `19`; selected/proposed routes, the player marker, and map projection remain independent assertions.

Startup non-blocking regressions require the i18n mutation observer to replay only affected subtrees without rewriting `document.title`. Loading may advance only from real completion signals for modules, world setup, opening current/next roads, selected-quality shader compile/link diagnostics, the first frame, and the animation loop; percentages, estimated time, and timer-only progress are forbidden. Both opening roads must avoid synchronous full builds, complete through bounded critical jobs in priority order, and reveal atomically. Runtime must wait for both readiness signals, compile the selected-quality shaders asynchronously, present a successful first frame, and start animation before atomically publishing an enabled Start. Update-lease coverage must prove that normal Continue publishes only the ready launch surface without unlocking audio, while `autostart=1` never flashes Start and retains loading until the first gameplay frame succeeds; any fatal path leaves Reload alone. World coverage also locks pooled roots hidden until their first real placement and visible afterward; candlelight coverage locks the immutable-topology marker on every shared geometry so `modelDebug` does not rescan pool clones. Node contracts prove scheduling and state boundaries only; a real page must still demonstrate a responsive main thread during startup and no wait window between visible launch and actionable Start.

Surface regression covers three contract layers. Gameplay core proves that the exact ellipse/rectangular-footprint Minkowski sweep cannot tunnel through a small puddle/snow patch during a `300m/s × 45ms` frame, including diagonal boundary/tangency, forward/reverse symmetry, lateral travel, inside endpoints, invalid/overflow inputs, reusable output, and `2_000` deterministic randomized reversal fixtures. Ship tests lock the immutable collider/pressure footprint and paired tail-downwash points. Weather tests lock `24m` stable buckets, at least `96m` behind, phase-safe `1_104m / 1_488m` mobile/desktop preview (`3.68s / 4.96s` at `300m/s`), and hidden negative-distance startup buckets. They also require spray to scale continuously from swept coverage, longitudinal/lateral penetration, pressure, speed, and landing energy: grazing cannot trigger a whole packet, tail points are tested separately, and contact/jet counts stay at or below `12 / 8` particles. Remaining coverage locks open-sky, tunnel, and `0.58m` downwash boundaries; the local `1.2m/s²` cap; lateral-multiplier floors; bounded `[0,1]` water displacement, wave energy, snow compaction, cleared grooves, and left/right berms; identical six-second recovery at `30 / 60 / 120Hz`; and a frozen zero-delta step. World tests separately lock low-discrepancy near/mid/far prefixes inside unchanged rain/snow capacity, `300 / 210m` desktop/mobile far-band horizontal half-spans, unchanged one-rain/two-snow draw groups, and the committed-route `12–30m` local mask: an ordinary bridge may hide nearby drops but must retain distant exterior rain; a sealed tunnel hides visible rain while the raw pool continues, and camera preview cannot shelter early. Forced `300m/s` samples validate sweep, layout, and pressure bounds only rather than a normal gameplay target; sustained average surface deceleration over `60s` must stay at or below `0.9m/s²`. Render contracts also lock high-albedo opaque snow sheets, receiver-only sheets versus casting volumetric banks, and no more than six participating mobile mixed-surface groups. Audio tests distinguish spray, snow compression, dry crunch, and darker slush; rain coverage locks light-detail, steady-curtain, and low downpour-body layers, proves full outdoor rain under an ordinary bridge plus `18% / 10%` low-pass mountain/underground transmission, caps light rain at `16%` of downpour and total weather gain at `0.032`, and retains one graph with zero per-frame allocation throughout continuous transitions.

Shoulder-snow regression additionally locks paired left/right banks per bucket, sampled-`roadHalf` placement, and zero added draw groups. Pure geometry mirrors left/right grazes, proves deep overlap is stronger than a graze, accepts `0.30m` downwash, and rejects `0.581m`, disabled, insufficient-snow, sheltered, and non-overlap cases. Stable cells keep bank compaction/displacement in `[0,1]`, freeze on pause, and recover identically at `30 / 60 / 120Hz`. Runtime must publish current-frame bank contact, cumulative interactions, disturbed-bank cells, swept hits, and bank-powder bursts. These checks prove contracts only; visible lowering, outward displacement, grooves, and powder still require WebGL inspection.

Vertical-launch regression separately locks both manual `SPACE` and the touch button to the same authoritative `jump() → launchVertical()` path. An executable runtime fixture covers flat pavement, a `3%` structural grade, and a `28%` supported physical-ramp plane at AT Gear 2 and `150km/h`. Every surface must accept the first launch, reject a second airborne request, and preserve speed, gear, and transmission mode. Sloped cases must resolve initial vertical velocity as the support-tangent component plus the projected active impulse while keeping `jumpPlatformFlightActive=false`, so deliberate flight cannot impersonate natural lip release or acquire cross-road landing authority. Gameplay-core fixtures apply the production impulse derived under standard `9.81m/s²` gravity from exact floor contact at `22 / 30 / 60 / 120Hz`; the ship must leave the floor on the first frame, reach an approximately `4.6m` apex, and land after roughly `1.94s`. The same cases retain immediate `t=0` floor contact for resting and descending bodies so excluding a separating root cannot weaken ordinary collision.

Cross-carriageway platform landing additionally uses one immutable destination-road handshake as the shared physics, collision, and presentation authority. Preflight must begin on entry to any corridor containing a physical platform; platform support and lip release remain disabled until the destination tile's ordinary signature has visual, collision, and prior-successful-frame presentation readiness. A blocked check must precede every write to `grounded`, takeoff counters, last-takeoff metadata, and swept-flight state while publishing a blocked count and status. The destination stays pinned during grounded prewarm, then source and destination tiles stay pinned together in flight, and collision explicitly consumes that same destination token. Safe landing, a genuine off-road crash, and reset all release the handshake. Opposing-route adoption always rejects an invisible landing surface, while a real geometric miss still follows `off-road-crash` rather than receiving synthetic pavement. `markPresented()` may commit only after a real presentation frame succeeds, so object construction alone cannot masquerade as a road the player has seen.

Longitudinal-propulsion regression locks the three full-throttle stages to natural equilibria near `110 / 160 / 280km/h` under one rolling, linear, and aerodynamic resistance equation. Releasing W immediately removes excess pedal thrust; grounded Gear 1 with zero pedal, no brake, and no torque interruption slowly approaches the real dry-level idle balance near `8km/h`, idle force is strictly zero from `16km/h`, S stops completely, and Gears 2/3, shifts, and airborne motion receive no hidden creep. Gear fixtures cover the `70 / 140km/h` upshift minima, `125 / 190km/h` safe-downshift ceilings, speed-preserving rejection, the `0.30s` torque/RPM interruption, and squared lugging ramps across Gear 2's `35→70km/h` and Gear 3's `90→140km/h` intervals. Runtime coverage proves T transmission and M full-throttle preference form four independent combinations: MT retains player Q/E with M on or off; AT removes desktop and mobile Q/E from layout, the accessibility tree, and pointer activation before automatically completing `1→2→3→2→1` across the `90 / 70km/h` and `140 / 120km/h` hysteresis pairs. Returning to MT atomically restores both button pairs, and repeated round trips must not duplicate event bindings. Spawn-pressure regression replays a real `1→2→3` trajectory and preserves event order, random seeds, distance, and fixed-step guard state across `30 / 60 / 120Hz` render partitioning.

Automatic timing is separately locked at `120Hz`: T automatic transmission shifts `1→2` at `90km/h`, `2→1` at `70km/h`, `2→3` at `140km/h`, and `3→2` at `120km/h`, retaining two `20km/h` anti-chatter bands. P's ordinary `120km/h` cruise normally remains in Gear 2, while uncapped full throttle reaches Gear 3 in one deterministic order. Executable presentation coverage requires visible `1→2 / 2→3 / 3→2` shift text on desktop/touch, a localized short rejection reason, and every manual gear input hidden, disabled, and unfocusable under AT; the measured mobile selector height and grid row count must actually shrink. MT restores visible controls without changing the live-flight sequential-focus policy. Both instruments must publish MT/AT, all three recommended working bands, exact automatic thresholds, and mode-aware `lugging / stalled` copy. The rejection's `1_600ms` wall-clock lease remains visible until expiry and refreshes exactly once at the boundary; repeated failures renew the full lease, pause cannot freeze it, last-rejection diagnostics persist, and any continuing lugging/stall must immediately retake the instrument afterward.

Real-browser surface acceptance should run a local HTTP server and open `?autostart=1&auto=1&seed=2301&quality=high&weather=rain&cloverleafMove=straight&modelDebug=1`, then the same query with `weather=snow`. Open road must show airborne rain/snow in near, mid, and far depth while puddles, sheets, and banks are already present in the distant road view instead of appearing as a near-nose batch. A puddle-edge graze should create only local disturbance or a few one-sided droplets; deep contact should form small jets only at actually immersed tail points, alongside progressive exact-contact ripples and V wake. Snow still needs lowered/darkened sheets, paired grooves, side ridges, and powder. Deterministic stepping plus disturbed-cell diagnostics verify return/refill. Diagnostics must show matching contact serial, local-jet/grazing-no-spray/emitted-particle counts, disturbed cells, and effects while force coefficients stay bounded. Under an ordinary elevated bridge, contact resolves to `dry` and nearby drops may be masked, but distant exterior rain and outdoor rain audio must continue. A sealed tunnel hides visible rain while retaining a darker nonzero rain return, with covered-interaction violations at `0`. After a trusted gesture unlocks audio, listen separately to clear water, dry snow, and slush, then traverse rain, heavy rain, thunderstorm, and hail transitions: light rain should be gentle, steady rain continuous without masking the score, and downpour weight should come from a lower body rather than bright hiss. Node, screenshots, and diagnostic numbers cannot replace real WebGL motion or listening acceptance.

PC Ultra regressions cover shadow capability fallback, unchanged Low/Medium values, High-only isolation, post allocation/release, the explicit Bloom allowlist, reversible ship/world materials, and zero gameplay authority. Terrain coverage also locks the floating-origin uniform, absolute-world restoration, and non-camera-locked diagnostics; the browser fixture checks that procedural coordinates and physical world cells use the same origin after the ship advances, requires positive ecotone/mixed/boundary counts only at the forced transition midpoint, and permits all three to be zero at a pure-realm start. Map-heading assertions freeze the presented-map diagnostics and sole accessible DOM owner within one paused run generation, preventing any pre-restart diagnostic from being compared with a post-restart live node. `browser/Neon_Autopilot_V23_HighSpeed_DroneHeat.postprocessing-pixel-test.html` reads complete RGBA output from the same WebGL2 renderer, scene, camera, target formats, and 4× MSAA inputs, requiring exactly zero differing channels between the former work path, the deduplicated path, and a repeated render. Road integration locks zero aprons/lenses and zero junction shader ranges in the production merged batch, leaving route edge shells as the sole visible top authority. It audits `48` split/merge incident pairs at `0.25m`, totaling `9,292` stations, to prove same-height, zero-grade, zero-bank sibling shells plus direct/loop `164m / 164m` and `164m / 136m` throats followed by `120m` C2 bank transitions. Geometry contracts also cover `collectorLength=354m`, `collectorOffset=240m`, `directRadius=400m`, lower-collector `grade-limited-c2`, the `13.5m` flyover, and `≤6%` grades. Tunnel regression locks the continuous indexed shell, `≤2m` stations, `≤0.005m` chord error, shared portal/rib stations, `0.608719m` minimum complete-roof clearance, and `1.546623m` minimum sibling-road-shell clearance across all `16` portal frames; both values must stay `≥0.25m`. Gameplay unit and runtime contracts exercise whole-frame swept vertical contact against road and ceiling pass-through. Node/static results prove contracts, finite values, closed topology, and scheduling boundaries only; they are not browser visual proof. Real WebGL must still drive continuously through splits, merges, and vertical connections to inspect curvature, bank, cracks, shimmer, Z-fighting, black frames, shadow artifacts, pass-through, and tier-switch resource stability.

`SKY_TERRAIN_ART_CONTRACT` v2 construction coverage builds the six pure-realm and six ecotone templates with real Three.js at both High and mobile quality, for `24` audited geometries. Each feature suffix is independently welded and requires every edge exactly twice, with `0` boundary, nonmanifold, degenerate, duplicate, or nonfinite triangles; the central `±10m` corridor must remain quiet negative space. The suite also locks twelve prebuilt instanced layers, natural-dielectric `metalness=0`, no hot-frame geometry rebuild, and unchanged terrain-height, tunnel-cutout, road-collision, gameplay-RNG, and pool-capacity authority.

Fallback-interchange coverage executes the exact runtime Three.js construction block. It requires the `SKY_ROUTE_SCENERY_CONTRACT` v2 `16`-ring × `32`-side pier and `16`-point × `6`-axial-ring beam to be sealed manifolds, retain the local envelope consumed by the existing matrices, satisfy warm-stone `metalness≤0.03 / roughness≥0.78 / clearcoat≤0.04 / emissive=0`, and preserve fixed `4 / 2` capacities. A separate disposal fixture places the same contact shadow in active state and a pool containing duplicate references, invokes unload twice, and proves every bridge resource, shared shadow resource, and clone material disposes exactly once by identity while pool/entity references clear. A source gate also prevents retired hazard/pickup material arrays from returning to production startup allocation.

High-horizon regression constructs the six real Three meshes and requires one opaque `MeshStandardMaterial` per realm, three relief ranges with real radial depth and vertical span, `96` segments, `1_728` triangles, and zero instances, emissive, or owned lights. All `18` quantized profile signatures must differ. Identical configuration must yield byte-identical position buffers, and updates plus Low→High switching may neither rebuild nor mutate them. Each realm's sole morph target must directly reference the next topology-identical position/normal buffers, while an independent `neonV23NextHorizonColor` attribute references the next colour buffer. A simulated Three shader-chunk compile proves the shared WebGL1/2 colour-uniform injection and its exact synchronization with the realm blend. Stable presentation and transitions both measure exactly one draw group. Complete camera snapshots cover position, quaternion, scale, up, FOV, aspect, near/far, zoom, and projection across create, update, and tier switching; raising camera Y must still leave horizon world Y at `0`. Node proves geometry, material, budget, determinism, and camera-read-only contracts only; real slope lighting, atmospheric occlusion, and motion parallax still require WebGL visual acceptance.

Underground-enclosure regression now builds the real production `BufferGeometry` for all eight tunnels against desktop `2.5m` and mobile `5m` road sampling. The `0.22m` shoulder joint, `0.49m / 0.54m` wall centre/thickness, and `0.84m` roof inset must resolve to zero wall-foot gap, zero penetration, zero roof-wall gap, and `0.08m` of roof overlap. Every road-shell row must be merged into the enclosure stations, while enclosure stations remain `≤2m` with `≤0.005m` chord error. Underground portal ribs retain at least `0.02m` from the road joint. Terrain cutout version 2 has no fixed minimum radius; a standard loop derives exactly `5.60m`, cutting `0.18m` into the inner wall while retaining `0.36m` to the outer face. Browser diagnostics expose shared joints, gap, penetration, road-polyline alignment adjustment, roof gap, and portal clearance. These values prove only the structural contract; final acceptance must still traverse the `south + left` underground route through its portal, curve, grade transition, and exit to inspect shimmer, Z-fighting, cracks, or pass-through.

Real-speed regression locks a true `0` start, immediate removal of excess pedal thrust when W is released, Gear-1 idle creep as a force balance rather than a speed floor, monotonic drag outside the creep envelope, exact S braking and idle hold, P-Autopilot cruise around `120km/h` plus uncapped full throttle when P is off and M is on, dry full-throttle equilibria near `110 / 160 / 280km/h` for the three drive stages, and a `7.5m/s²` service brake. A separate unit conversion fixture retains `105km/h = 29.1667m/s` and `291.6667m` over ten visible seconds at `1 / 2 / 3 / 4 / 10 / 30 / 60fps`. Every finite visible frame is fully consumed through substeps no larger than `45ms`, and hidden documents cannot accumulate catch-up time. Speed streaks fade in from `70km/h` and reach full strength at `160km/h`; lateral motion and speed FOV also saturate there so gear 3 adds no synthetic speed cue. World-fixed metre marks, arrows, signs, and the map remain the visual references while the moving center-route glow, dots, and target ring stay absent.

Camera regression must cover both Top and Hood. Runtime contracts lock Top's `20–22.5m / 11–13.5m / 20–30m / 70°` tactical framing, zero camera synthetic cruise, and `30%` ship synthetic cruise/base hover, together with Hood's `2.05m` eye, `1.32m` forward offset, `1.18m` nose extent, `0.14m` clearance, `1.25m` sightline height, `46–76m` lookahead, `65°` FOV, and authoritative `state.lateral`. Base touch optical tests retain the capability-plus-aspect gate, `0.78 → 0.46` blend, Chase height `×1.06`, Close lookahead `≥×0.78`, Top `×1.18 / ×1.12` within `26m`, open-road gains `4° / 5° / 5° / 7°`, covered gain `4°`, and the `84°` base target. Separately, `driving-camera-framing.test.js` verifies Chase/Close rear safety on narrow fine-pointer desktops and HUD-constrained landscape: the greater of aspect and HUD-height weights applies over `50% → 30%` clear height, retaining `×1.32 / ×1.60`, the `9.2m` underground baseline, and equal-duration damping at `30/60/120Hz`. Final Chase/Close/Top eight-corner projection uses a normal `84°` ceiling and at most `100°` when necessary. Extreme attitudes and impossible HUD slits must distinguish `fits` from `viewportFits / hudOccluded / fallbackActive`; a canvas fallback cannot pass as HUD success. Film, Hood, active free-look, and its return phase stay excluded, and the guard cannot move or scale the hull or move the final lens. Browser samples must cover touch/non-touch, both orientations, drawer disclosure, bends, grades, flight, and tunnel entry/exit while preserving HUD safety, physical clearance, readable Top framing, a Hood view without synthetic hull motion, and unchanged quality buffers, craft, collision, and gameplay snapshots. A `4:3` iPad's small base touch gain does not exempt its measured HUD from validation; a single snapshot or view cycle cannot replace continuous acceptance.

`hood-collision-guide-v3` regression derives the authoritative collision dimensions from the ship module's literal `colliderHalf={0.72,0.58,1.36}` and requires them to remain exactly `1.44 × 1.16 × 2.72m`; runtime may not recompute them from visible `geometry/scale/userData` or a `Box3`. Presentation permits only four short corner marks and two lower rails along the committed rather than proposed `PathPlan`, forbidding a complete wireframe, far gate, centre axis, and centre card; the marks' interior and driving vanishing point must remain transparent. Desktop alone shows a width readout at the lower viewport edge clear of input, while mobile must hide it. Threat fixtures prove that the probe reads only the committed route and a copied authoritative motion snapshot and remains bounded by a finite `2.5s` warning window plus fixed iteration/sample budget. They cover normal ground travel, future takeoff from a ramp lip inside the window, an already-active airborne phase, ground intersection, ceiling intersection, and safe passage; collider envelopes must overlap laterally, vertically, and in time before entering amber warning at `2.5s` TTC or red danger at `1.2s` TTC. Static contracts exclude proposed routes, the map's distance-only nearest obstacle, and any long-span, high-density physics sweep; jump/airborne state, pose, speed, route, lateral position, lives, collision, control, and physics snapshots must be identical before and after the call. The guide appears only during active Hood flight and projects exactly once after camera update and before presentation. Invalid route bases, probes, ground/ceiling samples, airborne projections, or screen projections must fail safely to normal or hidden without viewport clamping. Source and CSS must also prove no animation. Real-browser acceptance traverses straights, curves, tunnels, ramp takeoff, and airborne segments continuously to confirm the six short marks follow the committed route, remain legible over bright and dark scenes, leave the central road unobstructed, strengthen for upper/lower-surface threats only inside the real TTC windows, keep the lower-edge readout clear of HUD/driving controls, and expose no readout on mobile.

Propulsion and steering runtime regression lives in `contracts/Neon_Autopilot_V23_HighSpeed_DroneHeat.propulsion-runtime.test.js`. One longitudinal resolver integrates interval-average pedal thrust, independent Gear-1 idle force, rolling, linear, and aerodynamic resistance, surface load, grade, and braking. Throttle release immediately removes excess pedal thrust, braking cuts idle as well, and RPM plus `exhaustEnergyNormalized` decay with authoritative residual rotation. Coverage locks Gear-1 idle launch, the `8km/h` balance, the `16km/h` cutoff, S stop-and-hold, Gears 2/3, shift, and airborne disconnection, monotonic coasting outside that envelope, the three `110 / 160 / 280km/h` full-throttle dry equilibria, a `0.30s` torque cut, speed-preserving unsafe-shift rejection, the Gear-2 `35→70km/h` and Gear-3 `90→140km/h` squared lugging ramps, all four MT/AT × M combinations, AT ownership of all three gears at the `90 / 70km/h` and `140 / 120km/h` threshold pairs, P-Autopilot cruise around `120km/h`, uncapped P-off/M-on full throttle, the `7.5m/s²` service brake, bounded cadence agreement, and no direct speed reward from candlelight. Ship coverage separately requires more than `97%` main-plume length and opacity on the first release frame, monotonic residual-spool decay, and zero actual thrust throughout the afterglow. Manual and Autopilot control continue to share the progressive steering actuator, planner rollout, portal prediction, and bounded substeps no larger than `45ms`; pause freezes the complete feedback state.

The ship-redesign regression also locks the `dawn-pilgrim-manta-reliquary-v2` five-lobe, three-pleat, dual-layer cape, two-eye spirit mask, zero modern armour panels, zero visible turbines, and `70%` quiet-surface contract. Every High/Mobile main-visual mesh must remain closed and finite, with zero boundary edges, non-manifold edges, or degenerate faces; real visible Box3 measurements for healthy, wounded, and critical forms must all remain inside the conservative covered envelope. Local assembly tests check the declared cape-root, cloth-layer, scarf, and main/side aperture clearances. Idle, full-thrust, full-steer, and braking fixtures transform real vertices for main, inner, both side, and both reverse plumes, requiring fixed root planes, no forward penetration through the aperture, and tail growth only along the exhaust direction. The fixed collider, all three life forms, growth colour, differential exhaust, reverse thrust, three rings, four wakes, speed lines, shield, top halo, quality switch, and idempotent disposal interfaces must remain present.

Candle-growth regression spans gameplay core, ship, audio, and runtime. Audio tests pin `V23-audio-13`, `V23-effect-assets-1`, and `authoritative-core-drive-v5`; they cover byte-identical OGG payloads, zero `file://` fetches, release-keyed requests, corrupt HTTP-200 fallback, resume/decode races, the `1_000ms` TTL, and cancellation of scheduled thunder at hidden/lifecycle boundaries. Synthesis requires both exact copies to fail.

Score regression pins `V23-music-library-4`, `V23-music-rhythm-4`, and `V23-music-player-8`. Preview reuses production flight gain; `modelDebug=1` must read the player's existing mode/track as its initial state while making storage writes no-ops. The synthetic layout fixture retains zero played-event counts and remains a negative unlock check, never listening evidence.

- The Autopilot Liquid Glass static contract locks the seven-command deck to its own `16–26px / 420ms` upward transform on launch/resume/relaunch, an immediate Reduced Motion endpoint, and no transform on HUD/map/fixed positioning ancestors; `.auto-panel` remains the sole backdrop layer with no nested status blur; four named animations declare only `opacity / transform`; `30% / 58% / 82%` risk marks and all four risk/authority/braking/threat state rails remain complete; desktop/iPad receives the full reveal while phones keep a static `9px / 10px` summary; reduced-transparency, no-backdrop, and forced-colors fallbacks remain. Source assertions prove only selectors, declarations, and DOM-state contracts. They do not prove compositor use, stable animation frames, backdrop optics, or real-device GPU cost; reveal timing, settled state, occlusion, and touch layout require the browser matrix, while performance and visual quality still require physical-device acceptance.

## 积水真实感回归 / Realistic puddle regression

积水单元回归锁定 `PUDDLE_OPTICS_CONTRACT` 的中性介电水、`64 / 128px` 移动/桌面程序贴图、羽化岸边、中心到浅岸粗糙度、交叉微波法线、路线桶稳定旋转、窄路 `roadHalf` 收边、毫米级波面与向外扩散，并要求降雨按暂停时钟复用同一波纹池生成确定性稀疏雨点环。测试同时禁止地表天气模块重新引入 `AdditiveBlending` 或旧的 `22mm` 整片下沉，并要求视觉升级新增绘制组为 `0`。真实浏览器雨天验收还必须目视确认水坑轮廓互不整齐复制，湿路到水心过渡柔和，斜视角能看到天空/灯光随微法线破碎，雨点环持续但克制，受扰水面不会整块升降，波纹、尾流和水滴在暗处没有青色自发光；截图与源码断言不能替代动态 WebGL 观察。

The surface regression locks `PUDDLE_OPTICS_CONTRACT` to neutral dielectric water, `64 / 128px` mobile/desktop procedural maps, feathered shorelines, centre-to-shore roughness, crossed micro normals, stable route-bucket rotation, narrow-road `roadHalf` containment, millimetre waves, and outward spread. Rain must also reuse the same ripple pool to emit deterministic sparse impact rings from the pausable clock. The tests prevent the weather-surface module from reintroducing `AdditiveBlending` or the old `22mm` whole-sheet depression and require `0` additional draw groups. Real-browser rain acceptance must still show non-repeating silhouettes, a soft wet-road-to-water-centre transition, broken sky/light reflections at grazing angles, restrained continuous rain rings, no whole-sheet rise/fall under contact, and no cyan self-glow from ripples, wakes, or droplets in darkness. Screenshots and source assertions cannot replace observing the moving WebGL surface.

## 地下隧道地貌隔离回归 / Underground terrain-isolation regression

渲染合同锁定基础与 High 地貌共用第 2 版裁切着色器、`16 × 12m` 已提交路线采样、后方/前方 `36 / 156m` 窗口、U 形墙内 `0.18m` 重叠，以及新增绘制组 `0` 和纯呈现权威。固定 `8.2m` 最小半径必须保持删除，标准环道半径由道路与墙体合同推导为 `5.60m`。运行时合同要求裁切在相机之后、真实光照之前更新，并只消费 `sampleSurfaceWeatherFrame` 的已提交 `PathPlan`。浏览器夹具检查合同版本、段数上界、激活状态与段数一致、最大半径非负；最终真实页面仍必须驶入左转地下隧道，目视确认地面、岩石和植被不再穿进地板，也没有罩外空环，再驶出洞门确认外部地貌恢复且控制台无 WebGL 错误。

Renderer contracts lock the shared version-2 base/High cutout shader, `16 × 12m` committed-route sampling, the `36 / 156m` behind/ahead window, `0.18m` inner-U-wall overlap, `0` added draw groups, and presentation-only authority. The fixed `8.2m` minimum must remain absent, and the standard loop radius must derive to `5.60m` from the road/wall contract. Runtime coverage requires the cutout to update after the camera and before physical lighting while consuming only the committed `PathPlan` through `sampleSurfaceWeatherFrame`. The browser fixture verifies the contract version, segment bound, agreement between active state and count, and a nonnegative maximum radius. Final real-page acceptance must still drive into the left-turn underground tunnel to confirm that terrain, rocks, and vegetation no longer enter the floor and no exterior halo surrounds the enclosure, then exit through the portal to prove exterior terrain returns with no WebGL console error.

## 高视角天气回归 / High-view weather regression

天气单元与运行时合同锁定 `OPEN_AIR_VISIBILITY_CONTRACT` 对配置可见度及雾/尘消光的连续合成、户外近雾上限 `18m → 4m`，并证明隧道暴露归一在完全封闭处恢复中性可见度。世界与浏览器夹具锁定 `WEATHER_VIEW_VOLUME_CONTRACT` 的最高 Top 相机 `26m`、向上射线 `14°`、最小垂直跨度 `72m`、冰雹/扬尘近中远层复用、扬尘贴地高密比例 `72%`、云层 `36m` 净空与 `45m` 最低云底。回归还必须证明对象池、容量和绘制组增量均为 `0`，且全部改动为纯呈现；最终真实页面仍需在 Top 视角逐项观察雾、暴雪、沙暴和强降水，确认高处没有体积断层或远景穿帮。

Weather unit/runtime contracts lock `OPEN_AIR_VISIBILITY_CONTRACT` to continuous authored-visibility plus fog/dust extinction, the outdoor near-fog change from `18m` to `4m`, and neutral visibility after full tunnel-exposure normalization. World/browser fixtures lock `WEATHER_VIEW_VOLUME_CONTRACT` to a `26m` maximum Top camera, `14°` upward ray, `72m` minimum vertical span, near/mid/far reuse for hail and dust, a `72%` dense ground fraction for dust, `36m` cloud clearance, and a `45m` minimum cloud bottom. Regression must also prove `0` added pools, capacity, and draw groups plus presentation-only authority. Final real-page acceptance must still inspect mist, blizzard, sandstorm, and heavy precipitation in Top view to confirm there is no exposed volume boundary or distant weather break.

## 隧道真实灯具回归 / Authored tunnel-light regression

隧道灯光回归必须构建生产中的八段 tunnel profile，逐站确认每条路线独立密排灯罩/扩散片且整段全部保持可见发亮，并检查每条 authored emitter 的 tile token、edge、profile、station 与实体灯具一一对应。同一 station 还必须在既有道路、连续墙顶 shell 与肋架/灯壳实例写入有限、非负、无距离门控的静态照度；Low/Medium/High 都消费该属性，基础材质自发光仍为 `0`，新增 Mesh、绘制组、对象和真实灯均为 `0`。六盏复用聚光只补近距 PBR 高光，且仅两盏投影：普通驾驶要求玩家 edge/profile 精确匹配，Film 则选择摄影机最近的唯一 authored edge/profile 组；空记录、相邻路线、错误 profile 或已卸载旧记录都必须安全关闭，伪 emitter 兜底为 `0`。回归必须跨越旧 `144m` 边界、第七盏进入和 Film 跨组，证明 `24m` 尾带、旧槽渐出、零强度换位和新槽渐入均连续，同时 HDR/环境光和天体直射仍在洞门处连续衰减。普通开放桥底灯规则不变：弱太阳且 `underpassBlend > 0.68` 才渐亮，估算合计照度不超过太阳的 `32%`，晴朗白天为 `0`。最终真实浏览器仍须连续驶过山体廊道与地下隧道，确认没有灯具暗段、跳亮、错误隧道串光、结构自发白或洞门光照硬切。

Tunnel-light regression must build all eight production tunnel profiles, verify independently dense cover/diffuser fixtures at every route station with every fixture visibly lit for the complete span, and prove one-to-one identity between each authored emitter and its physical fixture through tile token, edge, profile, and station. The same stations must write bounded, finite, nonnegative, distance-independent static irradiance into the existing road, continuous wall/ceiling shell, and rib/housing instances. Low, Medium, and High all consume the attribute while base-material emissive remains `0`, with `0` added meshes, draw groups, objects, or real lights. The six pooled spots add only camera-near PBR detail, with two shadow casters: ordinary driving requires the exact player edge/profile, while Film selects one camera-nearest authored edge/profile cluster. Missing records, adjacent routes, wrong profiles, and unloaded old records must fail closed with `0` fake-emitter fallbacks. Coverage crosses the former `144m` boundary, a seventh fixture, and a Film identity change to prove the `24m` tail, old-slot fade-out, zero-intensity rebind, and new-slot fade-in remain continuous while HDR/ambient energy and celestial direct light still attenuate through the portal. Ordinary open-bridge soffit rules remain unchanged: weak sun plus `underpassBlend > 0.68`, estimated combined illuminance at or below `32%` of the sun, and `0` in clear daylight. Final real-browser acceptance must still drive continuously through mountain galleries and underground tunnels to exclude dark fixture gaps, popping, wrong-tunnel spill, self-lit structure, and hard portal-light transitions.

## 路面画质层级回归 / Road-surface tier regression

道路视觉回归锁定三档共用同一暖灰 road shell、`roadCoord`、碰撞与天气权威，但呈现成本分层：Low 必须选择 `low-physical-without-coordinate-shader`，不得带 normal/roughness map，且继续显示 LineSegments 标线；Medium 必须选择 `standard-road-coordinate`，以无贴图坐标 shader 提供 `45m` 接缝、轮迹、路肩和距离衰减；High 必须选择程序化 normal/roughness map、细尺度坐标 shader 与 PBR ribbon 标线。切换 `low / medium / high` 只允许改预建材质和标线可见性，不能重建 edge、改变 `PathPlan`、碰撞或新增绘制组。静态合同能证明材质、shader、贴图与切档边界，雾中顶面/箱梁腹板层次、轮迹强度和闪烁仍须真实 WebGL 目视确认。

Road-surface regression locks one shared warm-gray road shell, `roadCoord`, collision, and weather authority while tiering presentation cost. Low must select `low-physical-without-coordinate-shader`, carry no normal/roughness maps, and retain LineSegments markings. Medium must select `standard-road-coordinate` and provide `45m` joints, wheel paths, shoulders, and distance fade through a texture-free coordinate shader. High must select procedural normal/roughness maps, the fine coordinate shader, and PBR ribbon markings. Switching `low / medium / high` may change only prebuilt materials and marking visibility; it may not rebuild edges, change `PathPlan` or collision, or add draw groups. Static contracts prove material, shader, texture, and switching boundaries; fog separation between the top and box-girder fascia, wheel-path strength, and shimmer still require real WebGL inspection.

## 桥墩逐实例回归 / Per-instance bridge-support regression

桥墩测试会真实构建立交视觉并逐个分解全部 `239` 根常驻柱、盖梁与基础的 `InstancedMesh` 矩阵，检查每根柱脚与采样地形相接、基础包住地表、柱顶位于盖梁承压区、倾斜桥面无缝、单侧传力盖梁无无效外悬且宽度小于 `10m`。几何合同锁定可见墩身半径 `0.92m`，同时要求道路规划继续使用 `1.24m` 支撑净空半径及完整 `2.2×1.05m` 基础包络。道路测试另遍历 `12` 种 movement 的全部 `816` 条运行时支撑记录，验证地面到柱顶的完整有限载荷路径、普通单柱盖梁与宽跨双柱门架；v5 对向回主路桥必须声明 `continuous-box-girder`，按 `88m` 支点节奏和 `176.01m` 两跨最大间距接受候选，并在既有结构批次生成 `24m` 腹板分段及支点横隔。同高桥板精确接触必须放行，侵入 `8cm` 和穿越下层道路必须阻挡。旧无 `verticalEnvelope` 的 X/Z 调用结果保持不变，真实地貌采样器返回非法高度时必须上抛，只有没有采样 API 的旧宿主才可使用有诊断的固定高度回退。

The support suite builds the real interchange visual and decomposes every `InstancedMesh` matrix for all `239` resident columns, caps, and footings. It proves terrain contact, footing containment, shaft-to-cap overlap, seam-free banked slabs, zero unloaded outer cap arm, and a sub-`10m` one-sided transfer member. Geometry locks the visible shaft radius to `0.92m` while road planning retains the `1.24m` support-clearance radius and complete `2.2×1.05m` footing envelope. Track integration separately visits all `816` runtime support records across the `12` movements and validates a finite ground-to-top load path, ordinary single-column caps, and wide-span two-leg portal bents. The v5 opposing return must declare `continuous-box-girder`, accept candidates on an `88m` support rhythm with a `176.01m` two-bay maximum, and emit `24m` fascia segments plus support diaphragms inside the existing structural batch. Exact same-height slab contact must pass, while an `8cm` penetration and a column crossing a lower road must fail. Legacy X/Z callers without `verticalEnvelope` remain behavior-compatible; an installed terrain sampler must throw on invalid height, and only a host with no sampler API may use the diagnosed fixed-height fallback.

## 随机跳台与跨路落地回归 / Random-platform and cross-road landing regression

路网集成测试要求旧 launch edge 基面完全平坦，同一图块/种子稳定发布跳台，同时在连续图块中覆盖多种宽、高、长和横向位置；平台生成不得消费玩法随机流。`300m/s` 只保留为高速坡唇扫掠压力边界；正常闭环另以 3 挡 `260km/h`、重力 `9.81m/s²` 和真实坡度参数化验证 `18×7.2×5.2m`、`24×9.4×6.8m`、`30×12.2×8.3m`、`36×10.6×9.8m` 四种楔体，并用共享横向执行器/积分器证明 `30 / 60 / 120Hz` 均可在落地前从实际平台位置到达对向 `-24m` 中心；禁止隐藏弹射冲量。对向 `opposing-intertile` 继续断言 `visualOnly=true`、`gameplaySurface=false`、`collisionEnabled=false` 且不直接进入 `PathPlan`，但查询必须把命中分类为 `opposing-counterflow`。解析后的 `opposing-crossover-profile-v5-local-merge` 必须逐米覆盖完整 `0–1,080m` 同几何代理，并锁定以下平面站位：`1,080–1,800m` 抬升、`1,440–2,160m` 的 `720m / 24m` C2 S 形并线、`1,800–2,040m` 的 `20m` 高平台、`2,040–2,760m` 下降，以及 `1,080–1,200m` 半宽 `8.4→5.6m`、`1,200–2,640m` 保持 `5.6m`、`2,640–2,760m` 放宽至 `8.4m`。在 `280km/h` 下，最大横向曲率不得超过 `0.000210/m`，竖向与三维法向载荷分别不得超过 `0.103g / 0.17g`；跨线边必须携带 `88m` 连续箱梁支撑合同并在 `2,760m` 后恢复普通主路。互补岔路落地提交该 movement，当前路保持原计划，无后继道路拒绝接管；接缝世界位置不超过 `0.001m`、朝向不超过 `0.5°`，采用计划后的烛光、障碍、地图与碰撞都沿新路线。直道岔路另锁定内侧 `2m`、外侧 `18m` 及 `7.2m` 铺装净空。道路池回归继续验证起飞源 pin、36 步延迟 current、`0 / 1` 变体优先级、坡唇同步工作量为 `0`、High 标线预制、就绪边界制动与 `grounded=true` 前原子提交。路外接触仍进入 `off-road-crash → endGame()`，原有顶棚优先级与 `22 / 30 / 60 / 120Hz` 手工起飞回归继续保留。

Track integration requires the former launch edge base to remain perfectly flat, deterministic platform publication for a tile/seed, and multiple width, height, length, and lateral variants across consecutive tiles without consuming gameplay RNG. `300m/s` remains only a swept-lip stress boundary. The normal loop parameterizes the `18×7.2×5.2m`, `24×9.4×6.8m`, `30×12.2×8.3m`, and `36×10.6×9.8m` wedges at Gear-3 `260km/h`, `9.81m/s²`, and their real grades; the shared actuator/integrator reaches the opposing `-24m` centre at `30 / 60 / 120Hz` with no hidden impulse. The `opposing-intertile` edge remains `visualOnly=true`, `gameplaySurface=false`, `collisionEnabled=false`, and absent from direct `PathPlan` membership, but a hit must classify as `opposing-counterflow`. Resolved `opposing-crossover-profile-v5-local-merge` coverage must preserve the same geometry pointwise over `0–1,080m`, then lock these planar stations: rise `1,080–1,800m`; one `720m / 24m` C2 S-merge over `1,440–2,160m`; the world-Y-`20m` crest over `1,800–2,040m`; descent over `2,040–2,760m`; half-width taper `8.4→5.6m` over `1,080–1,200m`; `5.6m` through `2,640m`; and widening back to `8.4m` over `2,640–2,760m`. At `280km/h`, maximum horizontal curvature may not exceed `0.000210/m`, and vertical/spatial normal loads may not exceed `0.103g / 0.17g`. The crossover must carry its `88m` continuous-box-girder support contract and return to ordinary main-road geometry after `2,760m`. A complementary-fork landing commits that movement, a current-road hit retains its plan, and pavement without a successor is rejected. Seams allow at most `0.001m` world-position and `0.5°` heading error, while candlelight, hazards, map, and collision follow the adopted plan. Straight forks separately retain the `2m / 18m` offsets and `7.2m` paved clearance. Tile-pool coverage continues to prove the takeoff-source pin, 36-step delayed current handoff, `0 / 1` opposing priority, zero synchronous lip work, High-marking prebuild, readiness-boundary braking, and atomic commit before `grounded=true`. Off-road contact still routes through `off-road-crash → endGame()`, and existing ceiling priority plus `22 / 30 / 60 / 120Hz` manual-launch coverage remains intact.

真实 THREE 回归还必须覆盖普通/对向图的合法 `9→8` 决策节点和 `10→8` 导流鼻变化、`normal → opposing → normal` 双向切换、以较小对向图直接创建 resident 后再激活普通图，以及超容量候选在任何道路、支撑或装饰指针交换前失败。较小变体的两个多余桥塔槽和两个反光标槽必须全部变成零矩阵，回切普通图后全部恢复为非零；四入口乘三种 movement 的 `12` 组图都必须保持 `decisionStructureNodeCapacity=9 / goreNoseCapacity=10`。

The real-THREE regression must also cover the legitimate `9→8` decision-node and `10→8` gore-nose change, a full `normal → opposing → normal` cycle, activation of the larger normal graph from an opposing-base resident, and rejection of an over-capacity candidate before any road, support, or decoration pointer changes. Both surplus pylon slots and both reflector slots must become zero matrices on the smaller variant and return to nonzero matrices on the normal graph. All `12` entry-by-movement graph families retain `decisionStructureNodeCapacity=9 / goreNoseCapacity=10`.

## 道路图块长航程构建回归 / Long-run road-tile build regression

苜蓿叶地图集成回归使用彼此独立的 idle、critical 和 retry 队列验证道路 builder 生命周期。慢速 `+2` builder 先完成一个 warm 阶段，再随路线进入 next/current；测试要求 builder 实例与已完成步数不变、idle 回调立即取消、单个 critical 回调推进恰好 `12` 步，并在路面完成前让地图合同明确报告 `presentationDegraded=current-road-resident-missing`，完成后原子清除降级。该 handoff 还会保留已经出队的旧 idle 回调，在 critical handle 建立后执行它，并要求 builder 仍为 `1` 步、critical 队列仍为 `1` 个且诊断完全不变。一次性异常用同一个原错误 stack 验证损坏 builder 被取消、首个退避后只重建一次并恢复；持续异常必须只产生三次 builder、两次递增退避，第三次把第一个 Error 对象原样抛给外层，且 resident、活动任务和重试缓存全部为零。资源所有权矩阵分别注入 `finish()`、完成后 `setRenderQuality()`、旧 resident disposer 和 recovery variant install 异常，核对失败 job/视觉/模板只释放一次、重试后只留一个 resident 资源、最终 dispose 令创建数与释放数严格相等。epoch 竞态矩阵分别保留已出队的旧 critical、idle 和 retry，执行 `empty → new route → old callback`，要求 jobs、steps、records、当前 handle 和完整 diagnostics 零变化，再证明各自的新 callback 正常完成。同步 scheduler 矩阵让 critical、idle 与 retry adapter 在调用栈内立即执行 callback；critical 递归完成 current/next 后只能保留一个真实 idle successor，idle 同步完成 `+2` 后必须零 schedule，retry 同步失败/恢复后也只能保留真实 idle successor，最终三者均为 `resident=3 / pseudoHandle=0`。另两项终止回归要求回调出队后 `pool.dispose()` 保持 `0` job / `0` visual / `0` resident，以及首次失败后空路线的迟到 retry wake 保持一次尝试和零发布。另一个 `120` 个唯一图块的连续航程压力回归在每次 current 转换后核对 `poolSize≤3`、`created-disposed===poolSize`、零失败/零残留就绪请求，并在 dispose 后要求全部视觉资源归零。Node 证据覆盖调度、身份、计数和资源所有权，不替代真实 WebGL 长航程的画面、GPU 内存与上下文稳定性验收。

The cloverleaf-map integration suite drives independent idle, critical, and retry queues through the complete road-builder lifecycle. A slow `+2` builder first completes one warm stage and then becomes next/current; the test requires the same builder identity and completed work, immediate idle cancellation, exactly `12` steps in one critical callback, an explicit `presentationDegraded=current-road-resident-missing` map contract before completion, and atomic degradation clearance afterward. That handoff also retains the already-dequeued old idle callback, invokes it after the critical handle exists, and requires the builder to remain at `1` step, the critical queue to remain at one callback, and diagnostics to remain byte-equivalent. A one-shot exception preserves the same original stack while cancelling the corrupt builder, rebuilding once after the first backoff, and recovering. Persistent failure may create only three builders and two increasing delays; the third failure must rethrow the first Error object unchanged with zero residents, live tasks, or retry cache. The resource-ownership matrix separately injects `finish()`, post-finish `setRenderQuality()`, old-resident disposer, and recovery-variant install failures. It requires one release per failed job/visual/template, one resident resource after recovery, and exact created/released equality after final disposal. The epoch race matrix retains dequeued old critical, idle, and retry callbacks across `empty → new route → old callback`; jobs, steps, records, current handles, and complete diagnostics must remain unchanged before each new callback completes normally. The synchronous-scheduler matrix invokes critical, idle, and retry callbacks inside the adapter call stack. Critical recursion may complete current/next but retain only one real idle successor; synchronous idle must finish `+2` with no schedule; synchronous retry failure/recovery may also retain only its real idle successor. All three end at `resident=3 / pseudoHandle=0`. Two terminal cases additionally require a callback dequeued before `pool.dispose()` to retain `0` jobs/visuals/residents, and a late retry wake after failure plus an empty route to retain one cancelled attempt with zero publication. A separate `120`-unique-tile journey checks `poolSize≤3`, `created-disposed===poolSize`, zero failures/readiness residue at every current transition, and zero visual resources after dispose. Node evidence covers scheduling, identity, counters, and resource ownership; it does not replace a real-WebGL long-run review of imagery, GPU memory, and context stability.

## 飞行评级回归 / Flight-rating regression

玩法纯核测试锁定 `flight-rating-v2` 的五项权重总和、`90 / 80 / 70 / 60` 等级边界、输入不变性、深冻结输出及零数据不产生非有限值。`12km` 安全矩阵明确验证 `0 / 1 / 2` 次有效扣血对应 `12 / 6 / 4km` 平均无损航段与 `84 / 64 / 53` 安全分；首次护栏扣血和首次障碍扣血必须同罚一次，护栏数必须是总扣血子集，不能重复罚分。固定样本还验证平均速度来自距离/有效时长、失败长腾空只进入事实而不加分、跳台安全落地率与成功秒数共同形成腾空项、路外坠毁把安全项限制到 `20`，以及无跳台/不足 `2km` 时正确排除维度并发布覆盖度。

启动页合同测试锁定结算节点唯一性、八项整局事实、五个 meter、护栏明细双语键、gameover 隐藏配置、内部纵向滚动和外置动作。五边形合同另锁定 `safety → pace → endurance → collection → airborne` 轴序、冻结快照投影、`0–100` 有限值边界、缺样本断边/不填充、动态双语摘要，以及不删除原 meter/证据行；浏览器布局矩阵覆盖桌面两栏、窄竖屏堆叠、矮横屏两栏和 forced-colors 系统色。`damage=guardrail` 浏览器场景连续调用真实护栏扣血入口，证明首次扣血生效、无敌期立即重碰被拒、保护结束后才能再扣，并在第三次实际扣血后读取冻结结算中的护栏 `3`、障碍 `0` 与平均无损航段文案。运行时源码合同验证该审计入口不能清除保护计时或帧锁，统一保护闸之后才提交护栏子类计数，落地/坠毁各提交一次累计量，未完成空中航段只补入事实，`endGame()` 在展示前冻结快照，语言切换只重绘，重开清零全部统计，最高速度从权威步更新，并把已提交 `PathPlan` 交给空中落地查询。Node 结果证明公式、状态边界与结构，不能替代真实手机/矮横屏滚动、可读性和游戏结束实际交互验收。

Gameplay-core tests lock the `flight-rating-v2` weight sum, `90 / 80 / 70 / 60` grade boundaries, input immutability, deeply frozen output, and finite zero-evidence behavior. A `12km` safety matrix proves that `0 / 1 / 2` accepted life-loss hits produce `12 / 6 / 4km` average damage-free segments and `84 / 64 / 53` safety scores. A first guardrail hit and first obstacle hit receive the same single penalty, while guardrail evidence remains a subset of total damage. Fixed reports also prove average speed derives from distance/active time, long failed airtime remains factual without earning credit, safe platform reliability and successful seconds jointly form the airborne dimension, an off-road crash caps safety at `20`, and missing platform/sub-`2km` collection samples are excluded with explicit coverage.

Launch contracts lock unique debrief nodes, eight full-run facts, five meters, bilingual guardrail evidence, game-over setup hiding, internal vertical scrolling, and actions outside the scroll owner. Pentagon contracts additionally lock `safety → pace → endurance → collection → airborne` axis order, frozen-snapshot projection, finite `0–100` bounds, missing-sample broken outlines with no fill, dynamic bilingual summaries, and retention of every original meter/evidence row. The browser layout matrix covers desktop two-column, narrow-portrait stacking, short-landscape two-column, and forced-colors system styling. Its `damage=guardrail` scenario calls the real guardrail-damage authority, proves that the first hit is accepted, the immediate protected repeat is rejected, later hits wait for protection expiry, and the third accepted hit seals guardrail `3`, obstacle `0`, and average damage-free-segment evidence. Runtime source contracts ensure that audit hook cannot clear the protection timer or frame latch and that guardrail subtype evidence commits only after the shared gate, alongside exactly-once landing/crash accumulation, factual-only unresolved mid-air time, snapshot sealing before game-over presentation, redraw-only language changes, complete reset, authoritative maximum-speed sampling, and committed-`PathPlan` airborne queries. Node results prove formulas, state boundaries, and structure only; real mobile/short-landscape scrolling, readability, and game-over interaction still require browser acceptance.

iPad-specific regressions pin driving controls to `80–88px`, direct/utility controls to `56–60px`, landscape to one action row, portrait to `2 × 2`, and the route slot to `64px`. Landscape shares the safe-area top rail with `12px+` wing gaps; portrait keeps at least `8px` below the head. On every dual-axis `700px+` touch viewport, the centred transmission selector must share the real safe bottom with the four bottom-row driving controls within `1px`, retain zero overlap through MT / AT / Film, and survive landscape-to-portrait round trips for `1024×768 / 1180×820 / 1194×834 / 1366×1024`. `1180×820 CSS at DPR 2` is explicitly the `2360×1640` device-pixel target; short phone landscape retains its existing split bay. The `820×1180` fixture also opens both tablet details, resizes the same document to `390×844`, and proves only the most recently opened navigation detail survives before restoring the tablet viewport.

## 区域静态景物 V2 回归 / Regional static-scenery V2 regression

目录单元回归必须以真实 `createRegionTemplates()` 计划逐一构建 High 与移动两档的全部 `48` 个新增族，而不是只数配置行：六境各恰好 `3` 个地标和 `5` 个环境，与 `30` 个配置基线族合计 `78` 个静态景物族；每境新增 ID 必须唯一、原创，并严格包含一个地表环境和一个 `air-far` 高空环境。每个计划都要深冻结且恰好生成 `base / trim / glow` 三个非空合并批，实际 part 必须覆盖巨构轮廓、质量-留白-质量构图、布旗/花瓣、抽象精神面具、烛星记忆、近景凹刻以及前/中/远空间层；风之记忆丝与烛星凹刻使用原创古代光器语义，`sunset-pilgrim-wind-ring` 只能由开放风环与编织暮霞花瓣构成，`woven-sail-canopy-petals` 必须保留收尖封闭轮廓、薄厚度和交错布褶，不得回退为航站、硬质翼片或装甲面板。ID、motif、pattern 和 part kind 一律拒绝 `lens / tracer / circuit / neon / industrial / turbine`。真实 THREE 集成还需验证每个合并批只含有限 position/index/normal、闭合端帽或封闭轮廓、非退化三角形、有限 bounds，且不因同组近景雕刻拆回逐 child 绘制。预算锁定桌面/移动每族 `5_000 / 1_800`、每活动境 `32_000 / 11_200`、同时可见 `18_300 / 6_300` 三角形和 `18` 批；这是画质第一的 v2 上限，旧低预算不再是验收目标。全部计划仍必须报告 `0` 场景灯、`0` 碰撞体、`0` 地图实体副作用、`0` 玩法/导航写入和 `0` 逐帧资源分配。

六境差异回归要逐项锁定各自的 palette、ground、air 与 silhouette profile，确保只共享语法而不共享同一轮廓签名。转场测试必须证明 `transitionEligible` 是唯一策略权威、地标永不跨境预告、允许转场的环境只按相邻境策略出现；净空测试用完整道路胶囊、真实 render-space 水平 bounds、运动包络、同侧 peer 圆和地表/高空许可带裁决，非法候选只能确定性外移或隐藏，不能缩半径、回原点或写路线。普通与 Film 驻留都消费玩家/真实摄影机视锥并集：地标 base 轮廓持续存在，trim/glow 细节在距玩家或摄影机 `260m` 内进入、直到两者都退出 `340m` 才关闭，转场中不得因单阈值抖动、换锚或丢地图记录。视觉浏览器矩阵至少覆盖 High 下六境纯境、六个相邻境过渡前/中/后，以及桌面 `1440×900`、超宽 `2560×1080`、手机 `390×844 / 844×390`、平板 `1024×768 / 820×1180`；每个尺寸都要以普通车头、固定路边、前置和高斜 Film 镜头连续观察多帧，检查巨构远读、大片留白、地表/高空层次、近景凹刻、批次材质、封口、穿模、Z-fighting、突然出现/过早消失、转场重复、控制台与 WebGL 错误。Node 只能证明目录、计划和预算，不能替代真实 WebGL 视觉验收。

Catalog unit regression must build every one of the `48` added families through real `createRegionTemplates()` plans in both High and mobile rather than merely counting configuration rows. Each of the six realms owns exactly `3` landmarks and `5` environments, forming `78` static-scenery families with the `30` configured baseline families; every realm's two new unique original IDs must include one ground environment and one `air-far` environment. Every plan is deeply frozen and emits exactly three nonempty merged `base / trim / glow` batches. Real parts must cover a monumental silhouette, mass-void-mass composition, cloth/banner petals, an abstract spirit mask, candle-star memory, close carved relief, and foreground/midground/horizon depth. Wind-memory threads and recessed candle-star carvings use original ancient-light semantics. `sunset-pilgrim-wind-ring` must consist only of an open wind ring and woven sunset petals; `woven-sail-canopy-petals` must retain a tapered closed outline, thin depth, and alternating cloth folds rather than falling back to an aerodrome, rigid fin, or armour panel. IDs, motifs, patterns, and part kinds reject `lens / tracer / circuit / neon / industrial / turbine`. Real-THREE integration must additionally prove finite position/index/normal data, closed caps or outlines, nondegenerate triangles, and finite bounds for every merged batch without turning close detail back into one draw per child. V2 locks desktop/mobile caps at `5_000 / 1_800` triangles per family, `32_000 / 11_200` per active realm, and `18_300 / 6_300` triangles plus `18` batches for the simultaneously visible set. These are quality-first limits; the former low budgets are no longer acceptance targets. Every plan still reports `0` scene lights, `0` colliders, `0` map-entity side effects, `0` gameplay/navigation writes, and `0` per-frame asset allocations.

Realm-difference regression locks the distinct palette, ground, air, and silhouette profile for all six realms so they share a grammar without sharing one silhouette signature. Transition coverage proves that `transitionEligible` is the sole policy authority, landmarks never preview across a boundary, and eligible environments use only the adjacent-realm policy. Clearance coverage consumes complete road capsules, measured render-space horizontal bounds, motion envelopes, same-side peer circles, and allowed ground/high-air bands; an invalid candidate may only move outward deterministically or hide, never shrink, return to origin, or write routing state. Ordinary and Film residency consume the union of player and real-camera frusta: landmark base silhouettes remain present, trim/glow detail enters when either viewer is within `260m`, and it closes only after both leave `340m`, with no single-threshold chatter, anchor replacement, or map-record loss during a transition. The visual browser matrix covers all six pure realms, all six adjacent transitions before/mid/after blend, and at least desktop `1440×900`, ultrawide `2560×1080`, phone `390×844 / 844×390`, and tablet `1024×768 / 820×1180` under High. Each size observes consecutive frames from ordinary nose, fixed-roadside, forward, and high-oblique Film shots for distant monumental readability, negative space, ground/air depth, close relief, batch materials, closed seams, clipping, Z-fighting, pop-in, premature recycle, transition duplication, console faults, and WebGL errors. Node proves catalog, plan, and budget contracts only; it cannot replace real WebGL visual acceptance.

## 烛光与同行者自然介电回归 / Candlelight and companion dielectric regression

烛光单元测试必须深冻结 `CANDLELIGHT_ART_CONTRACT` v2，并在真实预热、取得的全部 `12` 个可收集根和 `6` 个环境烛群上测得每个不透明材质 `metalness=0`；透明火焰、光环、尾迹、Bloom、拾取碰撞、对象池和零 Light 合同保持不变。立交集成测试必须证明同行者的生产 main-visual family 精确为 `ambient-cape-spirit-companion`，同时保留 V2 九件闭合几何、`48 / 28` 容量、原实例矩阵与无碰撞安全边。

Candlelight unit coverage deeply freezes `CANDLELIGHT_ART_CONTRACT` v2 and measures `metalness=0` on every opaque material across all `12` real prewarmed/acquired collectible roots and `6` ambient clusters. Transparent flame, halo, trail, Bloom, pickup collision, pooling, and zero-Light contracts remain unchanged. Cloverleaf integration must prove the companion's production main-visual family is exactly `ambient-cape-spirit-companion` while retaining the V2 nine-part sealed geometry, `48 / 28` capacity, original instance matrices, and collision-free safe edges.

## 六境障碍造型 V3 回归 / Six-realm obstacle-art V3 regression

障碍工厂回归必须以真实 Three.js 分别构建桌面和移动两档的全部 `30` 个家族，并要求 core / relief / cue 三类几何在全目录各有 `30 / 30 / 30` 个唯一哈希、同一境内各 `5 / 5 / 5` 个唯一哈希。每个不透明层都必须闭合、有限、非退化、`metalness=0` 且完整落在运行时既有 collider AABB 内；公共根保持只平移，旋转、脉冲和缩放只允许作用于透明、深度测试、无碰撞、不投实体阴影的效果层。提示几何必须是封闭薄记忆花瓣或由 `3–5` 段各自带端帽的星纹丝组成的真实断续形态，禁止回退到 `TorusGeometry`、统一 halo 或通用 loft。测试还锁定 `3` 个绘制组、`3` 种材质、预热后帧内零资源分配、确定性种子、取得/释放容量、幂等复位与六境 family/motion taxonomy；旧现代 gate/lift/shutter 名称只可出现在负向禁止列表中。

Obstacle-factory regression constructs all `30` families with real Three.js in both desktop and mobile quality. Core, relief, and cue geometry must expose `30 / 30 / 30` unique hashes across the catalog and `5 / 5 / 5` within each realm. Every opaque layer must be sealed, finite, nondegenerate, `metalness=0`, and wholly inside the runtime's existing collider AABB. The public root remains translation-only; rotation, pulse, and scale may affect only the transparent, depth-tested, collision-free effect layer that casts no solid shadow. Cue geometry must be either a sealed thin memory petal or a truly discontinuous star-stitch form made from `3–5` individually capped segments; `TorusGeometry`, a shared halo, and a generic loft are forbidden fallbacks. Tests also lock `3` draw groups, `3` materials, zero post-prewarm frame allocation, deterministic seeds, acquire/release capacity, idempotent reset, and the six-realm family/motion taxonomy. Retired modern gate/lift/shutter names may appear only in negative regression lists.

## 电影场景驻留与隧道恒亮回归 / Film residency and continuous tunnel-light regression

Node 合同必须锁定 `650m` Film 前置、`1_600m` 远裁面、真实摄影机 FOV/宽高比/朝向/有效雾远面驱动的地面视锥选择、移动/桌面 `1_280 / 2_048` 固定池、`2.4:1 / 32:9`、Film `84°`、两格卸载滞后、零逐帧选择分配，以及同一帧 `camera → terrain/tile → cutout/lighting` 顺序。测试还要求异常宽视口保留真实视锥并通过带诊断的呈现雾安全收敛，不能静默露底。浏览器夹具检查所选实例不超过池容量、所见实例数与所选单元一致、普通支持视口不触发收雾、覆盖失败为零。道路池用例覆盖摄影机内 `2_800m` 的普通 previous 保留、`+2` 图块 `2_400m` 进入和 `2_800m` 退出的显隐滞回、`2_200m` 地图活动图下限、authority pin 优先与 `resident≤3`。世界用例锁定地标 `1_800m`、其余景物 `1_440m` 前视与 `480m` 后留，生物 `960–1_320m` 生成；Film 六平面 `320m` 预热、`480m` 保留和 `1_800ms` 宽限期间必须保持同一锚点与地图记录，主要地标细节在摄影机或玩家距其 `260m` 内开启，直到两者都退出 `340m` 才关闭。灯光用例对同一 `constant-on` emitter 在不同玩家/洞门距离采样相同功率，保留错误 edge/profile 的 fail-closed，验证道路、连续墙顶和肋架/灯壳实例都接收无距离门控的 authored irradiance，并覆盖第七灯及 Film 跨 edge/profile 的固定六槽渐出/换位/渐入，同时锁定 `19` 绘制组预算。Node 不能证明真实 WebGL 无跳现或亮度连续；浏览器必须在 Film 固定路边、前置、高斜视角和隧道入口/内部/出口连续观察多帧，检查诊断、控制台、WebGL 错误与画面亮度。

Node contracts lock the `650m` Film lead, `1_600m` far plane, ground-frustum selection from real camera FOV/aspect/orientation/effective fog, fixed mobile/desktop pools of `1_280 / 2_048`, `2.4:1 / 32:9`, Film's `84°`, two-cell unload hysteresis, zero per-frame selection allocation, and same-frame `camera → terrain/tile → cutout/lighting` order. Pathological wide viewports must retain their real frustum and converge through diagnosed presentation-fog safety instead of silently exposing bare terrain. The browser fixture verifies that selected instances remain within capacity, visible instances equal selected cells, supported ordinary viewports do not clamp fog, and coverage failures remain zero. Tile-pool cases cover ordinary previous retention within `2_800m` of the camera, `+2` presentation entering at `2_400m` and leaving at `2_800m`, the `2_200m` active-map floor, authority-pin priority, and `resident≤3`. World cases lock `1_800m` landmark and `1_440m` other-scenery lookahead, `480m` scenery rear retention, and `960–1_320m` creature spawn. Film must retain the same anchor and map record through `320m` six-plane prewarm, `480m` retention, and `1_800ms` grace; major detail enters when either the camera or player is within `260m` and closes only after both leave `340m`. Lighting samples one `constant-on` emitter at different player/portal distances with identical power, preserves wrong-edge/profile fail-closed behavior, proves distance-independent authored irradiance on the road, continuous wall/ceiling shell, and rib/housing instances, and covers seventh-fixture plus Film cross-edge/profile fade/rebind within the fixed six-slot pool while retaining the `19`-group budget. Node cannot prove real WebGL pop-free continuity or brightness. Browser acceptance must observe multiple consecutive frames from fixed-roadside, forward, and high-oblique Film shots plus tunnel entry/interior/exit, checking diagnostics, console output, WebGL errors, and visible luminance.
