# World / 世界场景

## 中文

保存六境地貌、云层、景物和非碰撞空中生物。世界层消费路线、天气与光照快照，只发布视觉状态。

降雨不再局限于镜头周围几十米：原有一组雨线按 `60% / 25% / 15%` 低差异交错为近、中、远深度，基础雪粒子按 `70% / 30%` 交错为中、远深度，独立近雪层继续显示可辨认的六枝雪花。桌面水平周期跨度分别为 `82 / 240 / 600m`，移动端为 `58 / 168 / 420m`，远层水平半跨度为 `300 / 210m`。容量仍是桌面 `420` 雨线、`280+72` 雪粒子与移动端 `150` 雨线、`100+28` 雪粒子；雨保持一个绘制组、雪保持两个绘制组。各深度仍用绝对世界种子减相机世界锚点再按本层跨度循环，因此转向、暂停和高速移动不会把降水粘在相机上。雨池在所有路线继续推进，仅由当前已提交路线的封闭状态控制 `12–30m` 局部遮雨：普通高架桥下只隐藏近处雨线，桥外远景雨幕保持可见；封闭隧道在洞门过渡后隐藏本地与远景雨线。相机预览不能提前遮雨，容量、对象池和绘制组均不增加。

High 的天空合同由相机中心渐变、三层天气云和六境远景共同组成。云池继续预热并由共享天气快照唯一控制，不在帧内创建对象：移动端 `24`、桌面 `48` 组巨型云丘/云墙/高空薄纱，每组只用 `2–3` 个封闭云丘叠合，水平尺度 `28–62m`、垂直尺度 `5–22m`，最低真实云底保持 `45m`；物理法线、主光阴影与 High 密度衰减共同塑造暗云底，不使用自有感应灯。远方烛月也是常驻天空实体：一个真实球体月盘、一个柔光环、`7` 枚稀疏记忆星瓣和两层封闭云纱在启动期一次预热，始终相对 Film 相机稳定并只读主物理月光方向，新增本地 Light 为 `0`。远景按完整 `zone.atmosphere.farScenery` 标识确定性生成，六境各用 `96` 段闭环噪声形成近中远三层连续同心 relief，并把本境巨构与云山集中在 `25–45%` 的天际跨度，留下 `55–75%` 可航行开阔天空。三层共 `1_728` 个三角形，合并为每境一个不透明物理材质网格；常态与转场都只有一个绘制组。下一境 position/normal morph target 和独立颜色属性均在启动时预绑，Three r160 的 WebGL1/WebGL2 路径不叠透明网格、不重建几何、不逐帧写顶点缓冲。水平锚最多保留 `96m` 世界空间视差，Y 固定世界地面；全部是只读相机的可逆视觉壳，不注册地图实体、灯光或碰撞。

区域扩展严格消费 `NeonRegionScenery` 的最终六境分类：每境三个主要地标和五个环境造物，共 `48` 个真实 Three.js 根节点。桌面基础池为 `42+48=90`，移动端为 `36+48=84`；`2_700m` 境内的主要地标位于 `390 / 1_290 / 2_190m`，五个环境族在各自 `280m` 造物段使用固定 `28 / 84 / 140 / 196 / 252m` 偏移。主要地标的独立无雾剪影合同扩展为 `700–1_500m`，封闭表面细节遵守 `260m` 进入、`340m` 退出和 `0.36s` 渐变滞后。只有明确标记 `transitionEligible` 的环境造物可进入相邻境末段 `72m` 过渡，并遵守 `12 / 36 / 60m` 的既有预览中心。适配器显式实现最终全部造型 part kind，包括风之记忆丝线、内凹烛星刻纹和编织帆篷花瓣；花瓣使用薄而收尖的封闭挤出、环向展开与交替柔和倾角，不复用硬质平板轮廓。未知 kind 直接报错，不回退成通用箱体。最终合并几何与 `Box3` 回报真实三角形、绘制组、水平半径和运动包络，每个材质批次对应一个真实网格。

既有六境 `30` 个基线景物族统一冻结在 V2 真实实现合同：每族保留对象池接口，同时把配置身份统一为朝圣与记忆语义，包括 `twilight-pilgrim-terraces` 和 `ascending-memory-court`；共享收尾器另加入至少两件本境独有的封闭表面细节。旧发光镜片、速度箭头、现代看台和环轨轮廓已经分别重塑成带内凹回折的记忆水盆、九点柔和风纹花瓣、八点风化朝圣阶台、斗篷云纱与封闭星环记忆丝线；生产可见 family/role 禁止 `lens / chevron / grandstand / elevator / rail / panel / aerodrome / neon / industrial / turbine`。所有不透明景物材质 `metalness=0`，不允许玻璃和现代工业表面语汇；最终净空半径始终使用实测水平半径加完整运动包络，转场摆动也不能穿入道路或相邻造物。

世界池根加入场景时一律保持隐藏，只有首次真实放置完成路线采样、道路净空与可见性证明后才可显示。该初始化边界防止尚未分配的六境池在首帧编译和投影中同时参与，但不改变后续池复用、地图登记或区域显隐合同。

`SCENERY_TERRAIN_CONTACT_CONTRACT` V1 把绝对世界 `x/z` 上的运行时渲染三角地形采样设为唯一落地高度权威；提交路线只提供水平坐标、朝向和道路净空证据，`frame.y / surfaceHeight` 即使来自高架道路也不得抬升景物。`30` 个基线族对应的 `42` 个池根、区域 `48` 族中的 `36` 个地面族以及全部 `6` 组环境烛群都用完整可见 Three bounds 的最低点落地，允许误差仅 `0.001m`。基线转场保留水平运动包络，但改为接触平面不动的 Y 轴微摆；路侧生物同样使用真实地形而非桥面。区域目录中只有冻结名单内 `12` 个 `air-far` 族使用 `designed-airborne-terrain-relative` 语义，其完整下缘保持在 `max(真实地形, 最高桥面)+8m` 以上，不能让普通地面族借用悬浮例外。环境烛光在每次锚点绑定时对 `44` 个烛座逐实例采样：每个底座最低点独立贴合地形，并保持烛焰根部 `-0.016–0.032m` 的隐藏搭接/细缝范围；实例矩阵、bounds 和采样数组均在预热期保留，不增加帧内资产分配。非有限地形结果直接报错，不回退到道路高度或世界零面。

道路距离仍由提交后的完整路线采样和道路胶囊查询共同裁决：主要地标保持 `6m`、环境造物与每境一个环境烛群保持 `3m`，同侧有效包围圆边缘保持 `4m`；远空造物完整下缘高于最高桥面 `8m`。所有平移、摆动和转场运动包络先并入实测 bounds 再做净空，最多向外尝试 `42×4m`，仍不合格就隐藏，不用原点或估算半径回退。玩家前视按完整表面分别预热主要地标 `1_800m`、其余基线/环境/烛群 `1_440m`，越过飞船后仍保留 `480m`；Film/高速摄影机先用左、右、上、下、近、远六平面全部外扩 `320m` 的视锥获取根，再以六平面外扩 `480m` 和 `1_800ms` 时间宽限释放。可见根不得直接重绑新锚点：宽限结束后先完整隐藏一帧，下一帧才可同步根节点与地图记录。主要地标细节取飞船或摄影机到完整表面的最近距离，按 `260m` 进入、`340m` 退出渐变。摄影机合同只延长呈现生命周期，不能取得路线、碰撞、生成或天气权威；曲线和分叉不得用 route delta 代替物理距离，peer 冲突只仲裁当帧。全部视锥、球体、候选记录和锚点缓存仍在启动期预分配，帧内资产分配保持 `0`。

`getMapEntities()` V2 在保留小地图兼容字段 `radiusM` 的同时，为 Film 避让发布同一对象姿态帧更新的完整相机包络：`horizontalRadiusM / geometryHorizontalRadiusM / motionEnvelopeM`，精确的 `absoluteMinY / absoluteMaxY / centerY / halfHeightM`，以及计入运动余量的 `cameraClearanceMinY / cameraClearanceMaxY / cameraClearanceCenterY / cameraClearanceHalfHeightM`。基线、区域、环境烛群和生物使用同一字段合同；消费者只能读取 `visible` 记录，不能反写场景、路线或生成状态。

`NeonCreatures` 在启动期预热 `18×3=54` 个真实池根，并在地图冻结前逐根登记；只有提交路线采样和完整证明成功后才延迟领取。空中生物完整下缘高于最高桥面 `6m`，路边生物距所有道路胶囊 `6m`，大型/小群中心间距分别为 `120/45m`；首次领取窗扩大为 `960–1_320m`，速度前视使用 `max(240m, 3.2×速度)`，高对比低空生物仍遵守 `max(160m, 0.8×速度)`、左右 `12°` 的读取楔。只有相邻境的小群生物可进入最终 `72m` 过渡，三个既有实例分别位于距边界 `60 / 36 / 12m` 的 `24m` 分段中心；容量与领取节奏不变。已激活的空中与路边根在接近及越过飞船时保持同一锚点；只有完整 bounds 已退到飞船后 `320m` 并越过 `64m` 相机余量、离开六平面 `320m` 预热与 `480m` 保留视锥并耗尽 `1_800ms` 宽限，才允许回收。封闭隧道显示数恒为零。旧 `sky_manta` 和 `dark_dragon` 不再构建、登记或更新。世界只拥有根节点姿态和地图记录；生物与烛光工厂独占其几何、材质、池复位、质量切换和释放。V3 可见相交合同只允许同成员身体—面具、面具—眼睛以及主成员身体—附肢/尾部四类隐藏安装缝。三族原本互穿的附肢沿身体轴分层，标准轴向 spread 为 `0.34`，修复三族为 `0.92`；六个群聚族把次级成员移到主翼完整顶点包络之外，实际表面净距至少 `0.06m`。合并几何保留只读组件三角范围，测试对三档真实生产三角执行窄相交检查，安装缝以外允许的交叉上限为 `0`；全部动画顶点还须留在世界使用的水平/垂直运动包络内。

## English

Owns realm terrain, clouds, scenery, and non-colliding aerial life. The world consumes route, weather, and lighting snapshots and publishes presentation state only.

Rain is no longer confined to a bubble only tens of metres around the camera. The existing rain group interleaves near/mid/far depth at `60% / 25% / 15%`; base snow interleaves mid/far at `70% / 30%`, while the dedicated near layer keeps legible six-branch flakes. Desktop horizontal periods are `82 / 240 / 600m`, and mobile uses `58 / 168 / 420m`, with far-band horizontal half-spans of `300 / 210m`. Capacity remains `420` rain plus `280+72` snow on desktop and `150` rain plus `100+28` snow on mobile; rain still costs one draw group and snow two. Every band wraps an absolute-world seed minus the camera's world anchor using its own span, so turning, pausing, and high-speed travel cannot glue precipitation to the camera. The raw rain pool keeps advancing on every route, while only the current committed route controls a `12–30m` local shelter mask: under an ordinary elevated bridge, nearby rain is hidden but the exterior rain curtain remains visible; a sealed tunnel hides both local and distant rain after its portal transition. Camera preview cannot shelter rain early, and no capacity, pool, or draw group is added.

High's sky contract combines a camera-centered gradient, three weather-owned cloud layers, and six realm horizons. The cloud pool stays prewarmed and uniquely follows the shared weather snapshot, with no frame-time allocation: `24` mobile or `48` desktop monumental cloud hills, walls, and high veils, each assembled from only `2–3` closed hills at `28–62m` horizontal and `5–22m` vertical scale. Their real lower surface stays at or above `45m`; physical normals, main-light shadowing, and High density rolloff shape the underside without owned proximity lights. The distant candle-moon is also a resident sky entity: one real spherical moon, one soft halo, `7` sparse memory-star petals, and two closed cloud veils are prewarmed once, remain Film-camera stable, and only read the main physical moon direction while creating `0` local Lights. Horizons are deterministic from the complete `zone.atmosphere.farScenery` identifiers. Every realm uses `96` seamless samples across three concentric relief bands, concentrates its monuments and cloud mountains into `25–45%` of the skyline, and leaves `55–75%` open navigable sky. All `1_728` triangles merge into one opaque physically lit mesh per realm, so stable and transitioning presentation both cost one draw group. The next realm's position/normal morph target and independent colour attribute are prebound at startup; Three r160 WebGL1/WebGL2 paths use no transparent overlap, geometry rebuild, or per-frame vertex-buffer write. The horizontal anchor retains at most `96m` world-space parallax while Y stays on the world ground plane. This remains a reversible, read-only-camera visual shell with no map entity, light, or collision registration.

The regional extension consumes the final six-realm taxonomy from `NeonRegionScenery`: three major landmarks plus five environmental creations per realm, for `48` real Three.js roots. The desktop baseline pool is `42+48=90`, and mobile is `36+48=84`. Within each `2_700m` realm, major landmarks sit at `390 / 1_290 / 2_190m`, while the five environment families use fixed `28 / 84 / 140 / 196 / 252m` offsets inside each `280m` motif. A landmark's independent fog-free silhouette contract now spans `700–1_500m`; closed-surface detail enters at `260m`, exits at `340m`, and fades over `0.36s`. Only environments explicitly marked `transitionEligible` may preview the adjacent realm through the final `72m`, retaining the `12 / 36 / 60m` preview centres. The adapter explicitly implements every final part kind, including wind-memory threads, recessed candle-star carvings, and woven sail-canopy petals. The petals use thin tapered closed extrusion, radial deployment, and alternating soft lean rather than a rigid flat contour. An unknown kind throws instead of falling back to a generic box. Final merged geometry and `Box3` report real triangle, draw-group, horizontal-radius, and motion-envelope metrics, with exactly one real mesh per material batch.

All `30` baseline scenery families now share the frozen V2 real-implementation contract. Pool interfaces remain stable while configuration identity uses pilgrimage-and-memory semantics, including `twilight-pilgrim-terraces` and `ascending-memory-court`; the common finisher adds at least two realm-specific closed-surface details. Former luminous-disc, speed-arrow, modern spectator-tier, and orbit-track forms are physically reshaped as a returning-profile memory-water basin, a soft nine-point wind-memory petal, weathered eight-point pilgrimage steps, a cape veil, and closed constellation-memory threads. Production-visible family/role semantics forbid `lens / chevron / grandstand / elevator / rail / panel / aerodrome / neon / industrial / turbine`. Every opaque scenery material has `metalness=0`; glass and modern industrial surface language are forbidden. Clearance always uses measured horizontal radius plus the complete motion envelope, including transition sway, so animation cannot enter a road or peer footprint.

Every world-pool root enters the scene hidden and may become visible only after its first real placement passes route sampling, road-clearance, and visibility proof. This initialization boundary keeps unassigned six-realm pools out of first-frame compilation and shadow work without changing later pool reuse, map registration, or realm-visibility contracts.

`SCENERY_TERRAIN_CONTACT_CONTRACT` V1 makes the runtime's rendered triangulated-terrain sample at absolute-world `x/z` the sole grounding-height authority. A committed route supplies horizontal pose, heading, and road-clearance evidence only; its `frame.y / surfaceHeight` may never lift scenery onto an elevated deck. All `42` pooled roots representing the `30` baseline families, the `36` grounded families among the `48` regional families, and all six ambient-candle clusters align the lowest point of their complete visible Three bounds to terrain within `0.001m`. Baseline transition motion keeps its horizontal envelope but becomes Y-axis sway with a stationary contact plane; roadside creatures use the same terrain authority rather than deck height. Only the frozen list of `12` regional `air-far` families has the `designed-airborne-terrain-relative` semantic, keeping its complete lower edge above `max(real terrain, highest deck)+8m`; an ordinary grounded family cannot inherit that exception. Every ambient anchor binding samples all `44` candle bases individually: each base's lowest point lands on its own terrain sample while the flame root retains a controlled hidden-overlap/gap seam of `-0.016–0.032m`. Instance matrices, bounds, and sample arrays are retained during prewarm, adding no frame-time asset allocations. A non-finite terrain result throws instead of falling back to route height or the world-zero plane.

Committed full-route sampling and the road-capsule query jointly own every distance decision. Major landmarks keep `6m` from roads, environments and one ambient candle cluster per realm keep `3m`, and same-side effective bounding-circle edges keep `4m`. The complete lower edge of air-far scenery stays `8m` above the highest deck. Every translation, sway, and transition envelope is included in measured bounds before clearance. Placement searches outward at most `42×4m`, then hides a rejected root without an origin or estimated-radius fallback. The player's complete-surface forward prewarm is `1_800m` for major landmarks and `1_440m` for baseline/environment/candle roots, with `480m` retained behind the craft. Film/high-speed presentation first acquires roots in the real camera frustum expanded by `320m` on all left/right/top/bottom/near/far planes, then releases them only after leaving a separately expanded `480m` six-plane volume and exhausting `1_800ms` of grace. A visible root may never bind a new anchor directly: after grace it hides for one full frame, then synchronizes root and map record on the next frame. Major detail uses the nearer ship/camera surface distance with `260m` entry and `340m` exit fading. The camera only extends presentation lifetime and gains no routing, collision, spawning, or weather authority; curves and forks cannot replace physical distance with route delta, and peer conflicts arbitrate the current frame only. All frusta, spheres, candidate records, and anchor caches remain startup-preallocated, preserving `0` frame-time asset allocations.

`getMapEntities()` V2 preserves the minimap-compatible `radiusM` while publishing a complete Film-avoidance envelope updated in the same frame as each object pose: `horizontalRadiusM / geometryHorizontalRadiusM / motionEnvelopeM`; exact `absoluteMinY / absoluteMaxY / centerY / halfHeightM`; and motion-inflated `cameraClearanceMinY / cameraClearanceMaxY / cameraClearanceCenterY / cameraClearanceHalfHeightM`. Baseline, regional, ambient-candle, and creature records share this field contract. Consumers may read visible records only and gain no authority to mutate scenery, routing, or spawning.

`NeonCreatures` prewarms `18×3=54` real pooled roots and registers each one before the map list is frozen; acquisition remains lazy until committed-route sampling and the complete proof both succeed. An aerial creature's complete lower edge stays `6m` above the highest deck, a roadside creature stays `6m` from every road capsule, and large/small-cluster centers keep `120/45m` spacing. Initial acquisition now spans `960–1_320m` and uses `max(240m, 3.2×velocity)` forward read; high-contrast low-altitude roots still honor a `max(160m, 0.8×velocity)` reading wedge spanning `±12°`. Only a small cluster from the adjacent realm may enter the final `72m` transition; its three existing instances occupy `24m` segment centres `60 / 36 / 12m` from the boundary, with capacity unchanged. An active root keeps its anchor while approaching and passing the ship; recycling is allowed only after complete bounds lie `320m` behind the ship plus a `64m` camera margin, leave the `320m` prewarm and `480m` retained six-plane camera volumes, and exhaust `1_800ms` of grace. Sealed tunnels always show zero creatures. Legacy `sky_manta` and `dark_dragon` roots are no longer built, registered, or updated. World owns root pose and map records only; the creature and candlelight factories exclusively own geometry, materials, pool reset, quality switching, and disposal. The V3 visible-intersection contract permits only four hidden attachment seams: same-member body/mask and mask/eyes, plus primary-body appendage and tail sockets. The three formerly crossing appendage families are separated along the body axis, using axial spread `0.34` normally and `0.92` for the repaired three; all six flock families place secondary members beyond the complete primary-wing vertex envelope with at least `0.06m` of real surface clearance. Merged geometry retains read-only component triangle ranges; tests narrow-phase every production triangle at all three qualities, allow `0` non-socket crossings, and sweep actual animated vertices against the horizontal and vertical world-placement envelopes.

## 高视角天气体积 / High-view weather volume

`WEATHER_VIEW_VOLUME_CONTRACT` 以最高 `26m` 的 Top 相机和向上 `14°` 的观察射线为边界，把各天气粒子的相机包络最小垂直跨度统一为 `72m`。冰雹与扬尘复用既有近/中/远深度带，扬尘在填满高处包络的同时仍将 `72%` 粒子保留在贴地高密区；云层相对最高 `9m` 桥面保留 `36m` 净空，因此最低云底为 `45m`。所有跨度都复用原有池、容量和绘制组，额外三项均为 `0`，且只影响视觉放置与循环。

`WEATHER_VIEW_VOLUME_CONTRACT` bounds a Top camera up to `26m` high plus a `14°` upward view ray, giving every camera-enclosing weather layer a minimum `72m` vertical span. Hail and dust reuse the existing near/mid/far bands; dust fills the elevated envelope while keeping `72%` of its particles in the dense ground layer. Clouds retain `36m` above the highest `9m` deck, so the minimum cloud bottom is `45m`. Every wider span reuses the existing pools, capacity, and draw groups, adding `0` of each and changing presentation placement/wrapping only.
